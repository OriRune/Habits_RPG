# Plan3 execution log

One line per completed (or blocked) plan item, appended by `/fix`. Fresh sessions read
this instead of re-deriving state from git history. Newest at the bottom.

**Columns:** date · plan item · finding IDs · files touched · notes (deviations, discovered issues, blockers).

| Date | Item | Findings | Files touched | Notes |
|---|---|---|---|---|
| 2026-07-06 | 1.1 | MP-01 | src/net/cloudSave.ts, src/net/__tests__/cloudSave.test.ts | Sidecar keys `habits-rpg-last-synced-version` + `habits-rpg-dirty`; module-load dirty subscription; push-not-pull branch requires non-null local envelope (verifier catch). Verified fable PASS. Known one-time gap: pre-upgrade saves have no marker, so the *first* post-upgrade launch can't detect an offline session. Pull-path clearDirty not gen-guarded (microtask window, benign). |
| 2026-07-06 | 1.2 | MINI-01 | src/engine/crawl.ts, mining.ts, forest.ts; src/store/slices/miningSlice.ts, forestSlice.ts; src/store/shared.ts; src/net/coop/reduce.ts; MineRunOverlay.tsx, ForestRunOverlay.tsx; + 3 test files | Scout found TWO extra `'choosing'` writers beyond the finding's four: guest-side guardian detection in coop/reduce.ts (both modes) — guarded too (all six). Shared `boonConsolation` helper in crawl.ts (heal 15, gold 40). `skipMineBoon`/`skipForestBoon` + Skip button also rescue legacy saves already stuck with `pendingBoonChoice: []`. Verified fable PASS. Nit deferred: consolation is silent (no toast) — cosmetic. |
| 2026-07-06 | 1.3 | MP-06 | src/net/cloudSave.ts, src/App.tsx, src/components/settings/SaveConflictModal.tsx (new), src/net/__tests__/cloudSave.test.ts | Deviation: rejected keep-newer-by-`lastActiveISO` (date-granularity; fresh saves stamp today at init → heuristic would auto-pick a brand-new cloud row over weeks of local). Forced-choice dialog instead; auto keep-local only when cloud row is trivial. Nothing applied/owned/stamped until the player chooses; `startAutoSync` guarded while pending; `wipeLocalSave` now skips never-adopted (null-owner) saves (verifier catch — session expiry during dialog would have wiped local). Verified fable PASS. Deferred: keep-local stamps owner only on next launch's pull (needs push success signal — 1.4). |
| 2026-07-06 | 1.4 | MP-07 | src/net/cloudSave.ts, src/views/SettingsView.tsx, src/hooks/useCloudSync.ts (comment), src/net/__tests__/cloudSave.test.ts | `pushCloudSave(): Promise<boolean>` (true only when THIS call landed the envelope; no-envelope → true); sign-out warns via Modal on failed flush ("Stay signed in" / "Sign out anyway"). Closed 1.3's deferral: keep-local + auto-keep-local + no-row import now stamp owner only on push success (last one = verifier catch). No scout spawned — territory fully mapped by 1.1/1.3 briefs (deviation). Verified fable PASS. Deferred nits: sign-out modal wording overwarns after a CAS-conflict flush (progress already adopted, wipe loses nothing extra); no in-flight guard on handleSignOut double-click (cosmetic). |
| 2026-07-06 | 1.5 | MP-05 | src/net/cloudSave.ts, src/store/useToastStore.ts, src/net/__tests__/cloudSave.test.ts | Rollback notice (8s toast) fires at the actual reversion site — pullCloudSave's apply block, when dirty + sync-marker + envelope present — covering both the CAS-conflict re-pull and the startup cloud-moved-past-marker revert, incl. the finding's guard-blocked compound case (notice lands on the pull that actually reverts). `pushToast` gained optional `ttlMs` (default unchanged). Lying "merged local changes" comment replaced. No scout (territory mapped). Verified fable PASS; envelope-evicted false-positive predicate fix applied post-verify. Deferred (optional per plan row): field-wise max-merge of monotonic fields. |
| 2026-07-06 | 1.6 | MP-26 | src/net/cloudSave.ts, src/net/__tests__/cloudSave.test.ts | `pushCloudSave` split into coalescing wrapper + private `doPushCloudSave`: strict serialization, one shared trailing follow-up (re-reads envelope → sign-out flush captures late mutations), cached promise never rejects. pullCloudSave's 3 internal push sites call doPush directly — wrapper reentrancy (push → conflict re-pull → push) would self-await and deadlock; verifier confirmed that path was reachable via keep-local resolve + CAS race. No scout (territory mapped). Verified fable PASS, zero issues. Pre-existing (unchanged): stopAutoSync doesn't cancel in-flight pushes. |
| 2026-07-06 | 1.8 | MP-16, MP-17 | src/net/clock.ts, src/hooks/useCloudSync.ts, src/net/__tests__/clock.test.ts | Finite guard + epoch-plausibility floor (`>= 1e12`) on server_now — floor added past the finding's shape after verifier flagged `''`/`true` coercing to finite 0/1 → "1970-01-01" keys; kept `Number()` coercion (NOT `typeof === 'number'`) since PostgREST may serialize int8 as string, with a test pinning that. Re-sync on tab-visible + hourly with proper effect cleanup (StrictMode-safe). MP-17 hook wiring has no automated test — repo has no jsdom/renderHook infra; verifier eyeballed it. No scout (26-line file). Verified fable PASS. |
| 2026-07-06 | 1.7 | ARCH-01 | src/store/slices/coreSlice.ts, src/store/useGameStore.ts, src/store/__tests__/resetGame.test.ts (new) | Picked up mid-flight (code fix pre-existed uncommitted; test/log were missing). Added `ownedGear: []`/`equipment: {armor:null,trinket:null,tool:null}` to resetGame's reset object, matching economySlice.ts's own initial values. Exported `createGameStore()` factory from useGameStore.ts (singleton unchanged) so the new test can spin up an independent pristine store and assert `resetGame()`'s post-mutation state deep-equals it (function/action fields stripped before comparing). Both store instances share the persist storage key, and the polyfilled localStorage hydrates synchronously, so the test clears localStorage before each `createGameStore()` call to prevent the reference store from rehydrating the mutated instance's state. Confirmed non-vacuous: reverting the two added lines makes the test fail on `ownedGear`. Verified opus PASS-WITH-NITS: deep-equals approach only guards fields the test explicitly mutates (covers ARCH-01's fields; a hypothetical future field left both unreset and unmutated wouldn't be caught) — accepted as-is, not worth the scope to enumerate every field. |
| 2026-07-06 | 1.9 | ARCH-08 | src/store/__tests__/persistMigration.test.ts (new) | Test-only; zero production changes — migrate/merge reached via `createGameStore().persist.getOptions()` (public zustand API). Scout premise correction: migrate has NO version branches — one unconditional idempotent field-presence backfill; fixtures reconstruct what each era LACKED. 7 tests: v3/v6/v24-era fixtures, idempotency (double-migrate no-op), populated-modern-save preservation (verifier catch — an always-re-derive statLevels mutation survived the era fixtures and would wipe veterans' allocations), merge live-run-wins for ALL six run fields by reference, merge cold-boot adopt. Verified fable PASS. |
| 2026-07-06 | check 1 | — | src/net/cloudSave.ts, src/net/__tests__/cloudSave.test.ts | Phase close-out: full suite 1296/1296, typecheck clean. code-health-auditor cross-item sweep: 1×P1 FIXED — deferred successful pushes (autosync completing a failed keep-local/import) stamped sync markers but never OWNER_KEY → fully-synced unowned save dodging sign-out wipe + account-switch reset; fix = adopt-if-unowned in both push success arms (single stamp point; per-path conditional stamps removed), regression test added. Test-hygiene fix: conflictStash now reset via wipeLocalSave() in beforeEach. Deferred P3s: transient-run-key list duplicated in 4 places (consolidate in Phase 7); cloudSave.test.ts payload-capture + scaffolding cleanup (fold into 2.9); handleSignOut warn-branch untested (no component-test infra; also 2.9); sign-out modal over-warns after conflict-flush (already logged at 1.4). Phase stamped ✅. |
| 2026-07-06 | 2.1 | MINI-02, MP-03, MP-04, ARCH-25a | src/engine/mining.ts, src/engine/forest.ts, src/engine/arena.ts, src/store/slices/miningSlice.ts, src/store/slices/forestSlice.ts, src/store/shared.ts, src/store/runRng.ts, src/store/useGameStore.ts, src/hooks/useMiningLoop.ts, src/hooks/useForestLoop.ts, src/hooks/useTacticsCoopSession.ts, src/net/coop/reduce.ts, src/net/coop/protocol.ts, src/components/mining/MineRunOverlay.tsx, src/components/forest/ForestRunOverlay.tsx, src/components/arena/ArenaOverlay.tsx, src/store/__tests__/timebase.test.ts (new), src/store/__tests__/runRng.test.ts, src/store/__tests__/persistMigration.test.ts, src/net/coop/__tests__/reduce.test.ts, src/store/__tests__/store.integration.test.ts | Convention installed: engine consumes injected rAF nowMs only; wire stamps + guards use Date.now(); overlays read engine stamps via performance.now(). mineStrikeCharged/mineCast/forestAct/forestActCharged/forestCast gained nowMs (uncharged forestAct included — its tracer stamp is unconditional, beyond the finding's literal list). MP-03 guard replaced with acceptTacticsStateT high-water mark in runRng.ts (reset on SUBSCRIBED); tactics-state + world slices stamped Date.now(). rebaseMineRun/rebaseForestRun called from persist merge (adopted runs only; live runs untouched — ARCH-08 test updated from toBe to toMatchObject). Scope extensions, same class: ArenaOverlay frozen/poison/ring renders (sweep find) and rebaseArenaRun (verifier SHOULD-FIX — arena is persisted and rAF-stamped too). Verified fable PASS-WITH-NITS; nits logged not fixed: (a) 1 ms Date.now() resolution can drop the 2nd of two same-ms tactics broadcasts (self-healing — full snapshots), (b) MonsterSlice.readyAtMs still ships host rAF stamp over the wire (inert — guests never step monsters). Residual: old-host+new-guest tactics pairing keeps MP-03 behavior until both peers update (no PROTOCOL_VERSION gate exists; 2.5 adds one). |
| 2026-07-06 | 2.2 | MP-02 | src/engine/forest.ts, src/hooks/useForestLoop.ts, src/net/coop/reduce.ts, src/net/coop/protocol.ts, src/net/coop/__tests__/reduce.test.ts, src/engine/__tests__/forest.test.ts | (a) Ranged scan extracted from act() into pure rangedScan/rangedBeastId (one source of truth — no drift risk); both guest attack branches route via guestTargetBeastId = facedBeastId ?? rangedBeastId through the existing AttackIntent send. (b) MonsterSlice/WorldSliceInput gained maxHp (optional on receive; key surfaced in WorldSliceInput — was already on the wire); buildWorldSlice sends maxHp; BOTH world merges rewritten intersection → host-authoritative rebuild (matched locals keep guest-visible status fields; missing-locally entities re-instantiated, maxHp ?? hp fallback for pre-maxHp hosts, no-key entities skipped). Floor-regen id matching safe (ids deterministic from per-floor seed); guardian boon detection unaffected (reads pre-merge list vs host ids). Verified fable PASS-WITH-NITS; charged-branch double-evaluation nit fixed post-verify. FOLLOW-UP FINDING (verifier, pre-existing, uncited): guest SPELL kills (forestCast/mineCast → castSpell → killBeast/killMonster) still resolve locally → transient divergence + loot dup + possible guardian boon double-fire; rebuild now heals the divergence but intents should carry spell damage too — fold into 2.4 (intent validation) or a new row. Also noted pre-existing: same-frame dash+act stale-snapshot race (self-heals); guest intents carry no stamina check (melee had this too; trust-model consistent). |
| 2026-07-06 | 2.4 | MP-11, MP-13, MP-23 | reduce.ts, hexBattle.ts, reduce.test.ts, hexBattle.test.ts | MP-11: chose interim clamp (plan-authorized) not the {charged} wire-shape change — extracted `clampRemoteDamage(dmg,maxHp)` shared by both apply fns. Note: kill removes the entity, so clamp-to-maxHp is unobservable in real data (bounds overflow/absurd values only, NOT single-target one-shot); full host-side recompute deferred. Test uses inflated-HP fixture (hp>maxHp) to make the clamp observable on the surviving path. MP-13: hoisted targetability onto the acting hero via shallow `anchored={...state,player:heroToCheck}` in playerAttack+playerCastSpell (preserves state ref on no-op). MP-23: roster-membership guard atop resolveTacticsIntent (skips solo/legacy no-roster). Verified opus PASS-WITH-NITS; stale docstring nit fixed. FOLLOW-UP (pre-existing, out of scope): useTacticsCoopSession.ts:80 host-side ownership check only rejects heroId===host-userId, never asserts msg.heroId===msg.userId — guest A can act as guest B (distinct impersonation finding; roster guard passes since B is a member). Candidate for a new MP row / 2.7 anti-abuse. |
| 2026-07-06 | 2.3 | MP-08, MP-09, MP-10 | reduce.ts, session.ts, useCoopSession.ts, useTacticsCoopSession.ts, reduce.test.ts, store.integration.test.ts | MP-08: extracted pure `nextSessionState(prev,incoming)` (reduce.ts, type-only CoopSession import — no runtime cycle); setSession routes through it (resets `joined` when session id changes/disappears; also clears remotePlayers when not joined, mirroring setJoined(false)). startCoop/joinCoop order (setSession→setJoined(true)) preserved so deliberate joins re-set joined. MP-09: pure `shouldReapOrphan(session,myId,joined)`; useCoopSession discovery gained a local `applyDiscovered` that reaps my own unjoined orphan (endCoopSession + setSession(null)) at both fetch sites. DEFERRED (optional per finding): the belt-and-braces started_at age cutoff in getActiveCoopSession — plan row mentions it but finding marks it belt-and-braces; discovery reap is the "smallest" fix. Guest-facing zombie window (host never returns) remains by design. MP-10: hero-join handler resends current {tactics-state,t:Date.now(),state} via the local send closure when s.tactics already exists (rejoin/reconnect/2nd guest), instead of the no-op beginTacticsCoop; composes with 2.1 resetTacticsStateT on SUBSCRIBED so the reconnecting guest accepts it. Verified opus PASS-WITH-NITS. NIT (disclosed): MP-10's resend line is hook/network-bound with no pure seam — shipped a beginTacticsCoop no-op *contract* test (locks the invariant the fix depends on) rather than a fail-without-hook-fix test; a real seam needs a mocked Supabase channel. Tests: nextSessionState ×3, shouldReapOrphan ×4, beginTacticsCoop no-op ×1; 199 pass in touched files, tsc clean. |
| 2026-07-06 | 2.5 | MP-12, MP-24, MP-25 | miningSlice.ts, forestSlice.ts, shared.ts, protocol.ts, reduce.ts, session.ts, useCoopSession.ts, supabase/migrations/0010_coop_protocol_version.sql (new), reduce.test.ts, store.integration.test.ts | MP-12: fixed in beginMining/beginForest (covers host startCoop + guest joinCoop, both pass explicit seed) NOT joinCoop as the plan row worded — guard changed `if (s.mining) return s` → `if (s.mining && seed===undefined) return s`; energy-bail also clears an orphan (`return s.mining ? {...s, mining:null} : s`) to close the hole where a low-energy co-op joiner would keep the stale re-seeded orphan. MP-24: PROTOCOL_VERSION=1 const (protocol.ts); pure `canJoinSession(session,myVersion)` — null→false, missing/undefined version→true (legacy rollout tolerance), else strict eq; joinCoop refuses+notice on mismatch; createCoopSession stamps protocol_version; migration 0010 (deploy-ordering hazard documented in header: apply before client or the insert fails). MP-25: `TileSnapshot` wire msg + pure `diffMineTiles`/`diffForestTiles` (regen pristine from a FRESH mulberry32(floorSeed(baseSeed,floor)), value-compare via JSON.stringify; mine diff filters host tombstone markers) + `applyMineTileSnapshot`/twin + coopApplyTileSnapshot store actions. Trigger is guest-sent `SnapshotRequest` on SUBSCRIBED (NOT host-side isNew) so a quick refresh-rejoin still inside the roster timeout is backfilled — verifier SHOULD-FIX #1. Verifier SHOULD-FIX #2 (untargeted snapshot reverts in-flight peer dig) traced to a non-issue: diff carries only host-CONFIRMED changes, so an established peer's unconfirmed dig is absent from the diff; apply is idempotent — kept untargeted broadcast. Verified fable PASS-WITH-NITS; folded in nits: request-trigger (1), accurate energy-bail comment (3), forest no-energy test (5), tombstone filter (6). Residual NIT (4, unfixed, documented scope): one-shot snapshot dropped if host is on a deeper floor than the joiner (current-floor scope per finding; continuous world stream makes it unlikely). 216 pass in touched files, tsc clean. |
| 2026-07-06 | 2.6 | MP-14, MP-15, MP-19, MP-20 | useParty.ts, party.ts, PartyChat.tsx, supabase/migrations/0011_party_members_realtime.sql (new), stage7.test.ts, party.test.ts | MP-14: extracted pure questDeltaToReport (positive-only)/nextQuestBaseline (Math.max high-water — fixes uncomplete→recomplete +2). Cloud-rehydrate double-count (hole a) needed more than onFinishHydration: the useGameStore.subscribe fires DURING rehydrate()'s setState, before onFinishHydration — so gated the subscriber with a hydrating flag toggled by persist.onHydrate(true)→onFinishHydration(rebaseline+false). Verifier confirmed the ordering against installed zustand 4.5.7 source (onHydrate sync before set, onFinishHydration in a later .then, both fire on every rehydrate). MP-15: getMessages order desc + limit + .reverse() (restores the ascending contract PartyChat scroll-to-bottom relies on). MP-19: party_members postgres_changes '*' listener → reloadParty(); migration 0011 adds party_members to supabase_realtime publication (idempotent, mirrors 0002 — DEPLOY-STEP, apply in dashboard); partyActions.send now returns ApiResult<null>, PartyChat restores draft + useToastStore toast on !ok (was clearing unconditionally); input disabled while sending (verifier NIT — avoid clobbering mid-await typing). MP-20: both useParty effects keyed on session?.user?.id not the session object (TOKEN_REFRESHED churn); verified bodies need only user.id (username via getState(), no direct token use). Direct mode. Verified opus PASS-WITH-NITS; NIT2 (hook/component wiring untested — no renderHook/jsdom infra) left as-is, pure extraction non-vacuous. Tests: questDelta/baseline loop (fails vs old prev=now), getMessages order-arg + reverse (fails vs old asc+no-reverse). 20 pass in touched files, tsc clean. |
| 2026-07-06 | 2.7 | MP-18, MP-27, trust-model | supabase/migrations/0012_party_quest_predicates.sql (new), src/net/party.ts, src/hooks/useParty.ts, src/components/party/PartyQuestPanel.tsx, src/net/__tests__/party.test.ts, docs/trust-model.md | MP-18: 0012 adds `and (ends_at is null or ends_at > now())` to increment_party_quest's UPDATE + `expire_stale_party_quests(p_party)` (membership-gated, flips lapsed active→expired), called from reloadParty before getActiveQuest so a past-deadline quest reads null for all members (realtime fans out the flip); PartyQuestPanel shows `formatDeadline(ends_at)` via server-synced now(). MP-27: p_amount clamped. **Verifier SHOULD-FIX (fixed):** initial ceiling 100 under-credited quantity-kind quests (a single steps/minutes log delta >100 → RPC drops excess, reporter high-water baseline advances → permanent loss). Raised to 1e6 sanity bound (progress already target-clamped by least(); ceiling only guards the un-clamped contributions accumulator + int4-overflow). Verified opus PASS-WITH-NITS, nit fixed post-verify. **Live-DB verification (MCP, project rclrnxeazvlqenskaqzv): 0009 is NOT live** — no check_xp_rate_limit fn/trigger, no suspect column, live leaderboard view has no suspect filter. This CONTRADICTS the ad-hoc row below ("0009 confirmed already live" — that claim was wrong). Also confirmed NOT live: 0006 (contributions). Live: 0007, 0008(corrected), 0010, 0011. **DEPLOYED live 2026-07-06 (Orion approved "deploy + fix 0009"):** applied 0006→0012 in order (0012 needs 0006's contributions column), then a CORRECTED 0009 — trigger + suspect column verbatim, but step-4 view rewritten to 0008's client-correct shape + `where public_snapshot is not null and suspect=false` (the repo file's rename would 42P16-fail vs the live view). Also `alter function check_xp_rate_limit set search_path=public` (advisor WARN — repo 0009 omitted it; every other SECURITY DEFINER fn sets it). Patched repo 0009 file to match live (fresh-deploy-safe). Introspection confirms all objects: RPC has deadline+ceiling+contributions, expire fn, suspect col+trigger, view filters suspect. Security advisor after DDL: only pre-existing items (security_definer_view on leaderboard — intentional, row 26; the anon/authenticated SECURITY-DEFINER-executable WARNs blanket every party RPC, self-guarded by is_party_member). Test: 3 RPC-contract guards (client↔0012 name/arg). Tests+typecheck clean. **Manual playtest needed:** with a real party, complete a habit → quest progress increments (proves the live contributions UPDATE path); let a quest deadline lapse + reload → quest reads expired. |
| 2026-07-06 | 2.8 | MP-21, MP-22, MP-29(a,d,e,f) | src/net/coop/reduce.ts, protocol.ts, session.ts; src/hooks/useCoopSession.ts, useTacticsCoopSession.ts; src/net/coop/__tests__/reduce.test.ts | Delegated opus, fable verify (wire-shape). MP-22: pure `shouldBroadcastTactics(prev,next)` (JSON-compare after blanking selected/reachable/targetable+**effects**) skips selection-only churn + `tailTacticsLog(state,20)` (overlay shows last 4). MP-21: `WorldSlice.hostPaused?` (additive, no PROTOCOL_VERSION bump) stamped from `document.hidden`; guest exempts paused host from `pruneStalePlayers` eviction — but BOUNDED by `PAUSED_HOST_TIMEOUT_MS=120000` (verifier BLOCKER: unbounded exemption = immortal zombie when host closes a backgrounded tab, no bye/slice to clear flag; now reaped ~2min). MP-29(a): send/interval moved inside SUBSCRIBED cb — verifier SHOULD-FIX: reconnect re-fires SUBSCRIBED, added `if(interval)clearInterval` before reassign (else stacked 10Hz senders). MP-29(d): removed dead `WorldSlice.status`; (e) removed dead `'lobby'` session status; (f) floor/stage-mismatch+undefined-baseSeed now bails `return current` before merge in BOTH applyMine/ForestWorldSlice (twin). Verifier FAIL→fixed 4 (1 BLOCKER,2 SHOULD-FIX,1 docstring NIT)→re-verify PASS. MP-29(b) already fixed (both fetch paths guard `active`); MP-29(c) tombstone deferred (audit marks optional, self-heals ≤5s). 83+142 pass, tsc clean. |
| 2026-07-06 | 2.10 | MP-27 | docs/trust-model.md | Mostly landed during 2.7 (same MP-27 finding, trust-model half): documented 0009 (soft rate-limit + deployment note), grown leaderboard surface enumeration (XP/depth/consistency), clock caveats (MP-16/17 one-shot-sync + device-fallback), Option A restated. This item added the remaining HONESTY fix: the party-quest row previously implied the 1e6 `p_amount` ceiling caps a forged call — corrected to state the deadline predicate is the real guard while a single in-window forged call can STILL complete a quest (progress is target-clamped, targets are small), accepted under friendly trust. No scout (territory owned by 2.7). Doc-only, no tests/verifier. |
| 2026-07-06 | 2.11 | ARCH-16 | src/components/tactics/TacticsOverlay.tsx | One-line transcription fix (Direct): `activeAttackStat` for attacks now reads `weapon.attackStat` (the per-hero weapon already computed at :247 as `tactics.player.weapon ?? tactics.weapon`) instead of the state-level `tactics.weapon.attackStat` (:345) — so in co-op a hero carrying a different weapon than the host-state default gets correct weak/resist affinity arrows. Solo unaffected (the two sources coincide). No test: logic is an inline IIFE in a 700-line component, no jsdom/component-test infra, not worth extracting for a one-identifier fix (consistent with prior UI-only P2s). typecheck clean. Manual check: co-op hero with a differing weapon sees correct affinity arrows. |
| 2026-07-06 | 2.9 | MP-28 | src/net/coop/reduce.ts, src/hooks/useTacticsCoopSession.ts, src/net/coop/__tests__/tacticsMessageRouter.test.ts (new), src/net/__tests__/cloudSave.test.ts | Delegated opus, opus verify (net-touching P2). Scout scoped MP-28's ranked list DOWN: 2.1–2.8 already covered most of it (session.ts transitions→2.3 nextSessionState/shouldReapOrphan; quest-reporter delta→2.6 questDeltaToReport; cloudSave conflict/offline→1.1–1.6; floor-mismatch→2.8). Genuine remaining gap = (a) the tactics incoming-message handler was inline+untestable in useTacticsCoopSession's channel.on cb (where MP-03 stale-drop + MP-10 resend-on-existing-board live), (b) the cloudSave "nulls all TRANSIENT_KEYS" test was vacuous (asserted only localStorage unchanged, nothing about the push). Fix: extracted pure `handleTacticsMessage(msg, ctx: {isHost,userId,hasTactics,tacticsActive,accept}): TacticsMsgAction` into reduce.ts (established home for extracted co-op decision logic); descriptor union resend|begin|apply|intent|bye|ignore; hook now thin dispatcher. Stateful `acceptTacticsStateT(t)` STAYS in hook (guest-only, computed before dispatch, passed in as `accept`) so router is pure + watermark advances identically. Verifier traced all 4 message branches old-vs-new 1:1 (behavior-preserving), confirmed non-vacuity (inverting any guard fails ≥1 router test), and confirmed cloudSave test now reads real insert payload `payload.state.state[k]` (matches insert envelope shape) + fails if durableEnvelope stripping removed. Deferred (out of scope, no jsdom seam): sign-out-flush-failure ordering in SettingsView handleSignOut. 12 new router tests; 144 pass in touched files (router 12 + reduce 83 + cloudSave 49), tsc clean. |
| 2026-07-06 | check 2 | — | src/hooks/useParty.ts | Phase 2 close-out. Full suite: 58 files / 1388 tests pass; tsc --noEmit clean. Acceptance (all co-op, manual-playtest shaped) — evidence per item: host-refresh-doesn't-freeze-guests (2.1 timebase/MP-03 + 2.8 hostPaused), guest-rejoin-Tactics (2.3 HeroJoin-resend/MP-10 + 2.9 router tests), ending-raid-detaches-everyone (2.3 session lifecycle + 2.8 'active'|'ended'), cross-version-can't-corrupt (2.5 PROTOCOL_VERSION refuse + 2.2 host-authoritative merge + 2.8 floor-mismatch bail), chat-recent-messages (2.6 newest-100). code-health-auditor over the co-op/party/net cluster: CLEAN — twin symmetry intact (MP-29(f) bail identical both slices, 3 high-water guards one pattern), timebase convention holds, no dead code from 2.8/2.9 removals, all extracted helpers non-vacuously tested. One P3 found + FIXED here: deriveActivity() had no `tactics` branch → Hex Tactics players showed "Online"; added `if (s.tactics) return 'In Hex Tactics';` (useParty.ts:139). tsc clean after. **Pending manual playtests carried from items (need a real 2-device party):** 2.6/2.7 quest progress increments on habit-complete + deadline-lapse reads expired; 2.11 co-op hero with differing weapon sees correct affinity arrows; the 5 acceptance co-op flows above. |
| 2026-07-06 | 3.1 | HABIT-03 | src/hooks/useReminders.ts, src/store/slices/coreSlice.ts, src/store/shared.ts, src/store/useGameStore.ts, src/engine/habitHealth.ts, src/components/onboarding/ReminderCard.tsx (new), src/views/DashboardView.tsx, src/engine/__tests__/habitHealth.test.ts, src/store/__tests__/store.integration.test.ts, src/store/__tests__/persistMigration.test.ts | Direct (persist bump). Sub-part 1 (manifest/installable + SW registration) ALREADY shipped in 556aaf2 — nothing to do. Sub-part 2: `fireReminder()` prefers `serviceWorker.getRegistration().then(reg.showNotification)` (the only path that works on Chrome-for-Android; page-context `new Notification` throws there) → native → toast; used `getRegistration()` not `serviceWorker.ready` (ready hangs forever with no SW, e.g. dev). Still foreground-only (poll runs only while tab open — documented; true background push needs Periodic Sync/push endpoint, out of scope). Sub-part 3: persisted `reminderCardDismissed` (v28, defaults false via merge AND migrate stamp) + `dismissReminderCard()` mirroring hasSeenWelcome; pure `missedRecentScheduledDay(habits,today)` (3-day lookback ending yesterday, skips as_needed/times_per_week, frozen days count as logged); ReminderCard offers to enable the reminder after a real miss, gated `isToday && hasSeenWelcome && !enabled && !dismissed && missed` (mutually exclusive with WelcomeCard). Verified opus PASS, no material issues. 185 tests pass in touched files, tsc clean. |
| 2026-07-06 | 3.2 | HABIT-02, HABIT-17, HABIT-15 | src/engine/habits.ts, src/store/slices/economySlice.ts, src/store/shared.ts, src/engine/dashboard.ts, src/components/habits/HabitCard.tsx, src/views/InventoryView.tsx, src/engine/__tests__/habits.test.ts, src/engine/__tests__/dashboard.test.ts, src/store/__tests__/store.integration.test.ts | Delegated opus, opus verify (P1, store/engine). No persist bump (frozen log entries + streak field pre-existed). HABIT-02: `useStreakFreeze` now refuses (returns s, no consume) when `currentStreak===0` (can't protect a dead streak); new `useRecoveryElixir(habitId)` retroactively freezes the most recent missed scheduled day. New pure `mostRecentMissedScheduledDay(habit,today)` mirrors currentStreak's walk byte-for-byte → returns the exact day the streak breaks on (verifier confirmed the freeze-day==break-day invariant, else the item would be wasted); leaves lastCompletedISO untouched (retroactive, not a today-completion). HABIT-17: three in-scope UI/derived sites (dashboard topStreaks, HabitCard flame, InventoryView protectable filter+flame) switched from stale `h.streak` cache to live `currentStreak`; intentional cache readers (recomputeHabitBonus, cloudSave member_habits snapshot, PartyView shared snapshot) left alone. HABIT-15: recovery_elixir (already priced/shop-listed/boss-dropped, zero consumers) now wired via new "Repair a Missed Day" InventoryView section. Verified opus PASS, zero issues. 188 tests pass in touched files, tsc clean. Deviation: also rewrote the pre-existing positive freeze integration test (its fresh-habit/zero-streak premise was invalidated by the new refusal guard). |
| 2026-07-06 | 3.3 | HABIT-11 | src/engine/dashboard.ts, src/store/selectors.ts, src/engine/__tests__/dashboard.test.ts | Direct (engine-pure + trivial selector read; no scout — dashboard.ts territory mapped in 3.2). `atRiskHabits` now `.sort((a,b)=>currentStreak(b,today)-currentStreak(a,today))` so the recommendation names the longest streak, not array-order [0]. New URGENT_STREAK_THRESHOLD=7 / URGENT_HOUR=18 promotion branch (step 1b, above finish_focus/start_today) fires when top at-risk streak ≥7 AND nowHour ≥18 — closes the finding's masking gap (start_today at completedToday===0 previously hid the warning exactly when dangerous). Shared `streakAtRiskAction(habit,streak,freezes)` helper used by both the promoted branch and the any-hour fallback (step 4): message now names the streak length + appends "You have N Streak Freeze(s)." when owned. DashboardOptions gained optional `nowHour?`/`streakFreezes?` (default 0 — existing test call sites + non-selector callers unaffected); selectDailySummary passes `now().getHours()` + `inventory['streak_freeze']`. No verifier (P2, no persist/state/net mutation — selector change is read-only). 20 dashboard tests pass (6 new), tsc clean. |
| 2026-07-06 | 3.4 | HABIT-13 | src/engine/habits.ts, src/store/slices/habitsSlice.ts, src/engine/__tests__/habits.test.ts, src/store/__tests__/store.integration.test.ts | Direct (store-touching P2, opus verify). New pure `streakMilestone(n)` table (7→+25g, 30→+100g+1 freeze, 100→+500g+1 freeze; exact-equality). `completeHabit` converted to block body: reward granted inside the reducer (isToday-gated), distinct `useToastStore.pushToast('🔥 N-day streak! …')` fired post-`set` (store slices don't otherwise toast — new but no cycle, useToastStore doesn't import useGameStore). **Verifier BLOCKER (fixed): times_per_week counts WEEKS and holds across extra same-week logs → milestone re-fired every same-week completion, farming free freezes.** Fix: new exported `isDayScheduled(habit)` predicate; milestone path gated day-scheduled only (daily/weekdays/custom) — also fixes the "N-day" mislabel for week streaks. Re-verify PASS-WITH-NITS. Tests: streakMilestone unit (7/30/100 + non-milestones), store 30→freeze+gold, non-milestone→nothing, backdate→no fire (gold-asserted, non-vacuous), times_per_week 7-week streak→no bonus. 180 pass, tsc clean. **DEFERRED to 3.8:** day-scheduled uncomplete→recomplete re-mints the milestone bonus (base gold refunded on uncomplete but milestone gold/freeze added off-ledger, never clawed back) — pre-existing uncomplete/recomplete mint class owned by 3.8; fix there by persisting a milestone-granted marker on the log entry so uncomplete reverses it. |
| 2026-07-06 | 3.5 | HABIT-06, BAL-21 | src/store/slices/habitsSlice.ts, src/components/habits/HabitCard.tsx, src/components/habits/CompleteHabitDialog.tsx, src/store/__tests__/store.integration.test.ts | Direct (store-touching P2, opus verify). One "reward receipt" toast built from `completeHabit`'s ACTUAL granted values (post-gear xp, granted gold, isToday energy) — captured as `receipt={xp,gold,energy,color}` inside the reducer, pushed post-`set` via useToastStore: `+X XP[ · +Ng] · +1⚡`, or `… · logged late — no energy` when backdated (BAL-21). Covers both binary and quantity paths (quantity flows through completeHabit too). Removed HabitCard's old base-XP-only binary toast + its now-dead `computeXp`/`useToastStore`/`pushToast` imports (was the sole other toaster → no double-toast). CompleteHabitDialog preview relabeled "Reward:"→"Estimated reward: ≈ X XP" + note "Gear and comeback bonuses are added on top — the exact total appears when you log it" (kept as explicit estimate, still guides quantity input). Verifier FAIL→FIX: new tests used `Array.prototype.at(-1)` which breaks tsc under the ES2020 lib target (typecheck was run pre-tests, missed it) — swapped to `arr[arr.length-1]`. Re-verify clean. 2 new receipt tests (same-day +1⚡, backdated no-energy; both pin gold+energy segments, non-vacuous). 153 pass in touched file, tsc clean. |
| 2026-07-06 | 3.6 | HABIT-09 | src/store/shared.ts, src/store/selectors.ts, src/components/character/StreakBonusChip.tsx (new), src/components/character/HeroBanner.tsx, src/components/mining/MineRunOverlay.tsx, src/components/forest/ForestRunOverlay.tsx, src/views/DungeonView.tsx, src/components/arena/ArenaOverlay.tsx, src/components/tactics/TacticsOverlay.tsx, src/store/__tests__/store.integration.test.ts | Delegated opus, opus verify (store-touching P2). habitBonus (streak→minigame gold multiplier) was in zero .tsx. Extracted pure `habitBonusCounts(habits)→{tracked,healthy}` in shared.ts (recomputeHabitBonus now calls it, byte-identical); new `selectHabitBonusInfo(s)→{bonus,trackedCount,healthyCount}` in selectors.ts; new `<StreakBonusChip>` ("×1.15 Streak bonus — 3 of 4 habits on streak", `.toFixed(2)`, null at 1.0). Chip beside HeroBanner energy + on ALL run summaries. **Gold-parity is the core risk** — every panel now displays the POST-bonus figure `round(rawGold*habitBonus)` matching what commitRun/collectDungeon bank (verifier traced all 7 sources 1:1): Mine banking + death/tombstone, Forest banking + "Carried home" death (NOT "Lost to the wild" — forfeited, never banked), Dungeon ended RewardLine, Arena outcome (new gold line via `arenaReward`), Tactics outcome (new gold line via `tacticsReward`). Arena/Tactics previously showed NO gold. Verifier PASS-WITH-NITS → both follow-ups fixed (death panels under-reported + omitted chip; ×1.1→×1.10 padding). No persist change (habitBonus already a persisted character field, recomputed on load). Selector test: 4 daily habits streaks 5/3/4/2 + 1 as_needed streak 9 → trackedCount=4, healthyCount=3, bonus=1.15 (non-vacuous: fails if as_needed counted or threshold wrong). 154 pass, tsc clean. |
| 2026-07-06 | 3.7 | HABIT-10, HABIT-14, HABIT-23, HABIT-24 | src/engine/weekly.ts, src/engine/habitHealth.ts, src/components/weekly/WeeklyReportModal.tsx, src/components/weekly/PlanWeekModal.tsx, src/App.tsx, src/engine/__tests__/weekly.test.ts | Delegated opus, opus verify. No persist bump (all new WeeklyReport reads guarded optional). HABIT-10: `WeeklyReport.focusResults` (per focus-habit completed/target/pct) rendered as the FIRST block in the review. HABIT-14: habitHealth's HabitWarning gained `habitId`; `WeeklyReport.healthAction` surfaces the review's top structured action as ONE-TAP button — `mark_focus` handled in place (setHabitFocus); all other codes (change_frequency/lower_target/change_difficulty/suspend) navigate via new `onReviewHabit` prop → setTab('habits') (I caught the implementer's first pass rendering the button ONLY for mark_focus — the real warnings' primary suggestedActions[0] are never mark_focus; grep-confirmed against habitHealth.ts). HABIT-23: PlanWeekModal lists the weekly challenge rotation with one-tap startChallenge. HABIT-24: two motivational Balance tiles in the review (habitXpShare via selectBalanceReport, weekNet via buildEnergySummary). Display fix: "N logged" when scheduled===0. Verified opus PASS-WITH-NITS → nits polished. Tests in weekly.test.ts (focusResults eval + healthAction surfacing). tsc clean. |
| 2026-07-06 | 3.8 | HABIT-01, HABIT-04, HABIT-16, HABIT-05 | src/engine/habits.ts, src/engine/challenges.ts, src/store/slices/habitsSlice.ts, src/store/slices/challengesSlice.ts, src/store/useGameStore.ts, src/engine/__tests__/habits.test.ts, src/store/__tests__/store.integration.test.ts | Direct (persist bump 28→29). CRUX (energy anti-farm): a marker on the deletable log entry can't block re-mint (uncomplete deletes the entry), so added TWO per-Habit markers that survive the `{...h,log}` spread: `lastEnergyGrantISO` + `lastMilestoneGrant` (never cleared on uncomplete). HABIT-04: grant gated `lastEnergyGrantISO !== day` → complete→spend→uncomplete→re-complete nets 0 energy. HABIT-16: grant also gated `energy < MAX_ENERGY` so no marker set when +1 would clamp away → uncomplete never phantom-deducts at cap. HABIT-05: recovery now requires `mostRecentMissedScheduledDay(habit,today) > lastCompletedISO` (a genuinely-skipped scheduled day in the gap) — Mon/Wed/Fri with no skip no longer gets a permanent 1.1×; fresh habit short-circuits. HABIT-01: `clampReward` (new, challenges.ts) bounds custom-challenge override to suggestReward's [20,300] gold / [30,400] statXp; suggestReward refactored onto the same bound constants (behavior-identical). Closed deferred-3.4 milestone claw-back: uncomplete reverses gold + earnings.gold['habit'] + freezes (clamped ≥0); same-day re-mint blocked, later legit re-climb re-grants (date-keyed). Verified opus PASS-WITH-NITS. Sole NIT (accepted, intended anti-farm tradeoff, safe-direction never-mints): honest same-day complete→uncomplete→re-complete forfeits that day's energy and milestone reward. 200+12 tests pass, tsc clean. |
| 2026-07-06 | 3.9 | HABIT-07, HABIT-08, HABIT-19, HABIT-18, HABIT-20, HABIT-21, HABIT-12 | src/views/CreationView.tsx, src/views/__tests__/CreationView.test.ts (new), src/content/habitTemplates.ts, src/components/onboarding/WelcomeCard.tsx, src/components/habits/HabitForm.tsx, src/App.tsx, src/views/LoginView.tsx | 3 parallel implementers over disjoint clusters (scout mapped 5 clusters into 3 file-disjoint chunks). No persist touch → no verifier (top finding P2, UI/content-only). Cluster A (CreationView, opus): HABIT-07 quick-start was passing `allocations:{}`/`spellKey:''` → silently forfeited all 5 STARTING_STAT_POINTS + signature spell; new pure `quickStartAllocations()` = `{[getWeapon(STARTER_WEAPON).attackStat]:3, EN:2}` (weapon-derived so robust to a starter change) + `QUICK_START_SPELL=SIGNATURE_SPELL_CHOICES[0]` ('firebolt'); createCharacter untouched (its empty-spell tolerance test preserved). HABIT-19 disabled-CTA now shows "Choose a weapon and spell to begin" (text-ember) on first paint. HABIT-20 soft non-blocking "starting small works best" hint at selectedGroupIds.size>2. New CreationView.test.ts pins alloc sums to 5 across ≥2 stats + spell is a real signature choice (fails when helper→{}). Cluster B+C+D (copy/data, sonnet): HABIT-08 retagged beginner_fitness to 4 distinct stats EN/AG/HP/ST (was AG/AG/HP/HP, declared primaryStat EN untrained) — Walk→EN, Sleep→ST; HABIT-18 WelcomeCard drops the level-1-unreachable dungeon promise + rewords the XP para to the honest Level-5 boss gate; HABIT-21 uncapped label "unlimited — no XP cap" → "XP scales with amount, up to 10× target (normally capped at 150%)" (true UNCAPPED_RATIO_CAP=10). Cluster E (guest, opus): HABIT-12 mandatory account wall → component-local `guest` useState in App.tsx + `onGuest` prop/link in LoginView ("Play offline as a guest"); signedOut branch bypassed when guest; auth/cloud stay inert (key off authStatus not guest); sign-in-after-guest still works (guest flag never read on signedIn path; SaveConflictModal still honored); persist v29 untouched. Deviation: HABIT-20 uses the adjacent None-selected line's `text-ink-light/60` (parchment panel) not the finding's near-invisible `text-parchment-300/80`. tsc clean across all three; 160 tests pass in touched suites. Manual playtest (guest): backend-configured + signed out → Login shows guest link → tap → Creation/dashboard, cloud inert, later sign-in works. |
| 2026-07-06 | 3.10 | HABIT-22 | src/engine/dashboard.ts, src/store/selectors.ts, src/engine/__tests__/dashboard.test.ts | Delegated opus, no verifier (engine-pure reprioritization + dead-branch delete; selectors change is read-only derivation, no state/persist/wire mutation; 22 tests incl. ordering-lock). Root bug: every completion grants +1 energy → `energy_ready` (step 5) always preempted `all_done` (step 7) on a finished day, so the closure moment never fired. Fix: moved `all_done` above `energy_ready`; fully-complete day (pendingToday===0) now shows closure with an appended " You've got N energy to spend on an adventure." hint ONLY when currentEnergy>0. `energy_ready` still fires on partial days (pendingToday>0 && completedToday>0 && energy>=cost) — locked by a new ordering test. Deleted the dead `load_warning` branch + its `'load_warning'` union member + `DashboardOptions.loadWarning` field + selectDailySummary pass-through (grep confirmed DashboardView never consumed the kind). Tests: converted the old energy_ready-after-completion test → expects all_done + '3 energy' substring (the reprioritization proof); added zero-energy-no-hint + partial-day-energy_ready. tsc clean. DISCOVERED (logged, deferred to 7.6/ARCH-19 dead-export pruning): `selectHabitLoadWarning` (selectors.ts:109) is now an exported-but-unreferenced selector — its ≥12-daily-habits UI chip was never wired; left in place (no scope creep). If product wants the high-load nudge resurfaced, wire it to an always-visible chip — otherwise prune in 7.6. |
| 2026-07-06 | check 3 | — | — | Phase 3 close-out. Full suite: 59 files / 1430 tests pass; tsc --noEmit clean. Acceptance (all met, evidence): (a) external app-open cue → 3.1 PWA installable manifest + SW + post-miss reminder card; (b) missed day repairable + UI never baits a wasted item → 3.2 repair-most-recent-missed + refuse-consume-at-streak-0 + live currentStreak everywhere + recovery_elixir wired; (c) 7/30/100 feel different → 3.4 milestone toast+reward, hardened by 3.8 anti-farm; (d) every reward shows actual grant → 3.5 receipt toast from granted values + 3.6 habitBonus chip on all 7 run summaries + dashboard. Phase 3 not in {1,2,7} → no code-health-auditor sweep (per skill). **Pending manual playtests (Phase 3 is UI-heavy):** 3.1 install PWA on Android + reminder fires (foreground-only by design); 3.9 guest link (backend-configured + signed out → "Play offline as a guest" → Creation, cloud inert, later sign-in works); 3.5 receipt toast wording (+X XP · +Ng · +1⚡ / "logged late — no energy"); 3.10 all_done closure message appears on a fully-finished day (previously masked by energy_ready); 3.6 streak-bonus chip shows post-bonus gold. Phase stamped ✅ 2026-07-06. Carried discovered issue for 7.6/ARCH-19: selectHabitLoadWarning now orphaned (see 3.10 row). |
| 2026-07-06 | 4.1 | BAL-01 (P1) | src/engine/xp.ts, src/engine/habits.ts, src/store/slices/habitsSlice.ts, src/components/habits/CompleteHabitDialog.tsx, src/components/habits/HabitForm.tsx, src/engine/__tests__/xp.test.ts, src/store/__tests__/store.integration.test.ts | Direct (open-design decision → decided, then implemented; core-XP P1). DESIGN DECISION: habit XP = `base × (1 + 0.15×(L−1))` (new `LEVEL_XP_SLOPE=0.15` + `levelXpMultiplier(level)` in xp.ts, clamps sub-1 to 1.0). Chose the finding's exact recommended knob after reading BAL-01's math: 0.15 matches the trial per-energy slope at ~4 habits/day, lifting habit XP share from ~37%→~58% at L10 and ~25%→~56% at L20 (habits-primary restored across mid/late game) while flattening the 87-day L19→20 habit-only slog; self-limiting vs the L^1.5 threshold curve, not a runaway. Threaded optional `level?` through XpInput→computeXp (default 1 = today's numbers, so all existing callers/tests stay green) and resolveCompletion opts; habitsSlice.completeHabit passes `s.character.level`; CompleteHabitDialog preview reads character.level for parity. No persist change (statXp stays a number ledger; only future magnitudes change). Verified opus PASS-WITH-NITS. NIT-1 FIXED post-verify: HabitForm reward preview was showing flat base XP (a L10 player saw "20 XP" but earns 47) → now scales `BASE_XP×levelXpMultiplier(level)` (gear/recovery still land on top, disclosed). NIT-2 (accepted, logged): backdated completions score at CURRENT level not historical — safe-direction, matches pre-existing backfill-grants-XP behavior, no exploit (backfill guards unchanged). Blast radius confirmed nil: applyReward/commitRun/earnings ledger/balanceReport habitXpShare only SUM the already-computed xp — no double-application. Only 2 prod computeXp callers (both updated); HabitForm uses BASE_XP directly (the fixed preview). Tests: xp.test level cases (L10→47, L20→77, hard L20→135, backward-compat L1/omit→20, composes w/ ratio+recovery, levelXpMultiplier unit) + store.integration L10→statXp.KN 47 & earnings.xp.habit 47 (fails under old flat code). 206 tests pass in touched suites, tsc clean. |
| 2026-07-06 | 4.3 | BAL-03 (P1) | src/store/slices/economySlice.ts, src/store/shared.ts, src/views/InventoryView.tsx, src/store/__tests__/store.integration.test.ts | Delegated opus, opus verify (P1 store). SCOPED to shop+buyGear; mithril recipe DEFERRED to 4.4's designed batch (scout recommendation — avoids a one-off design 4.4 would reconcile). `buyGear(gearKey)` mirrors buyWeapon byte-for-byte (getGear lookup, price/owned/gold guards, unlimitedGold bypass, deduct exactly gear.price, push to ownedGear, NO auto-equip). Added to EconomySlice + GameState (shared.ts hand-mirrors slice action sigs — same pattern as buyWeapon; disclosed deviation, no persist change). InventoryView: gearForSale = priced && !owned GEAR, new Gear merchant block cloned from weapon-sale JSX (reuses gearCrest/gearBonusText), empty→renders nothing. iron_pickaxe (price 200) now buyable+equippable; mithril_pickaxe (no price) correctly stays out until 4.4. content/gear.ts + content/recipes.ts confirmed untouched. Verified opus PASS (zero issues). Tests: buy+equip iron_pickaxe (200→0, equipment.tool set) + insufficient-gold no-op & double-buy charged once (owned once, gold 300); fail-to-compile without buyGear. 161 tests pass, tsc clean, version still 29. |
| 2026-07-06 | 4.2 | BAL-04 (P1), MINI-06 (item half) | src/engine/bosses.ts, src/engine/arena.ts, src/engine/__tests__/arena.test.ts | Delegated opus, opus verify (P1 economy faucet). Root: createArena copied NAMED_BOSSES.rewards (500g + recovery_elixir/healing_potion at gate tiers) into repeatable arena payouts → arenaReward paid the one-shot boss table every ~3min win (2.5× gold sawtooth + repeatable named-item re-farm). Fix: extracted exported pure `genericBossReward(tier) = {gold:40+8×tier, items: tier%3===0?['healing_potion']:[]}` in bosses.ts (bossForLevel's generic fallback now calls it, behavior-identical); createArena sets rewardGold/rewardItems from `genericBossReward(opts.tier)` UNCONDITIONALLY — arena no longer reads boss.rewards at all. INVARIANT preserved: the one-shot level-up battle (battleSlice.ts:75-76) still pays named boss.rewards once per gate level (pendingLevelUp nulled pre-reward → non-farmable); NAMED_BOSSES untouched. coreSlice:139 devSpawnTrial is a battle spawn, not an arena consumer. Verified opus PASS-WITH-NITS. NIT-1 (accepted, logged): an arena run persisted mid-fight by pre-fix code keeps stale rewardGold=500 for that one in-flight run — self-heals on next beginArena, transient Option-A state, no migration. NIT-2 (deferred, cosmetic): genericBossReward(opts.tier) called twice at arena.ts:630-631 (throwaway alloc) — not worth hoisting a const into the ArenaState constructor for zero functional gain. Tests: 3 tier-5 assertions recomputed to the generic curve (win 80, death 20, retreat 40) + new tier-20 regression (named boss carrying 500+items → arena pays {gold:200,items:[]}, fails under old code) + genericBossReward unit. 48 arena tests pass, tsc clean. |
| 2026-07-06 | 4.4 | BAL-05 (P1), BAL-16, BAL-17, BAL-24 | src/content/gear.ts, src/content/recipes.ts, src/engine/__tests__/crafting.test.ts, src/store/__tests__/store.integration.test.ts | Design item (I decided the numbers), delegated sonnet (transcription once decided), NO dedicated verifier — content-data-only (no store/net/persist logic), craft-flow tests + tsc cover correctness; balance magnitude deferred to check-4 game-design-auditor parity pass. DESIGN: 2 new craft-only gear (no price) — `obsidian_plate` (armor, defense 12, ward 3; steps the BAL-17 stall from bronze's def 8; vs magma_colossus 20-touch → 8 through, meaningful not trivializing) + `resin_trinket`/'Amber Charm' (trinket, statBonuses EN 4 — first EN-granting gear, closes BAL-24). 3 recipes at 100-150g (vs old 20-40g band → late-tier sink, BAL-05) consuming 3 dead-end materials (BAL-16): mithril_pickaxe = obsidian×4+frost_quartz×2+150g (gives the orphaned mithril gear its recipe — last BAL-03 gap; gates on floor-15 obsidian), obsidian_plate = obsidian×3+frost_quartz×2+iron_bar×2+130g, resin_trinket = amber_resin×3+crystals×2+100g. Tool progression now clean: stone(pow1,free)→iron(pow2,200g shop)→mithril(pow3,craft). No engine/store/UI change (ForgeSection/GearSection/craft/aggregateGear already generic; GearDef accepts defense/ward/EN — tsc confirms). Stale recipes.ts header comment updated. Tests: 3 recipes resolve→real gear (crafting.test) + combined craft-flow deducting exact materials/gold per recipe (store.integration). NOTE (pre-existing, flagged not fixed): craft() doesn't block re-crafting an owned gear key (still deducts) — arguably BAL-05's intended repeatable sink; left as intentional. 169 tests pass, tsc clean, version 29 untouched. |
| 2026-07-06 | 4.5 | BAL-11, MINI-19 | src/engine/mining.ts, src/engine/__tests__/mining.test.ts | Delegated opus, opus verify (MINI-19 correctness inversion + co-op fan-in). Engine-only, mirrors forest killBeast. BAL-11: `killMonster` non-guardian branch now draws `bounty = def ? randInt(def.bounty[0], def.bounty[1], rng) : 0` and mergeReward's it into the drop → every non-guardian mine kill banks guaranteed bounty gold (dead `MineMonsterDef.bounty` field, values [1,4]→[40,70] across 8 monsters, was NEVER read; gold previously only appeared on a 1-in-N monsterLootPool gold pick, N growing 2→7 by floor). mergeReward SUMS gold (dungeon.ts:90), so a material pool pick still pays bounty and a gold pick stacks on top — pool pick kept as bonus. MINI-19: swingsToKill now divides maxHp by `weapon.attackStat==='DX' ? rangedPower : meleePower` (byte-identical to strike()'s split at mining.ts:898, was hardcoded `meleePower`) → archer (DX, meleePower 0) no longer hits Math.max(1,0)=1 → swings=maxHp loot inflation. killMonster signature unchanged (6 callers: strike/spell/poison/ring-of-fire/burn/damageMonsterById); co-op host-authoritative `damageMonsterById` reaches guests via existing broadcast, NO protocol field added, no persist change (Reward shape unchanged, version 29). Verified opus PASS (zero blockers): mergeReward additive confirmed, attack-stat expr matches strike() exactly, guardian branch untouched, def-undefined→bounty 0 no crash, both-powers-0 guarded. Tests: BAL-11 loops 30 seeds asserting banked gold >= cave_slug.bounty[0] (fails pre-fix on any material-pick seed); MINI-19 equal-power(20) melee-vs-archer(DX,meleePower 0) same seed asserts archer loot <= melee (pre-fix archer swings 40 vs melee 2 → fails). Stash-out control confirmed both non-vacuous. 29 mining tests pass, tsc clean. |
| 2026-07-06 | 4.6 | MINI-05 (P1), BAL-13, MINI-30 | src/engine/dungeon.ts, src/store/slices/dungeonSlice.ts, src/store/__tests__/store.integration.test.ts | Delegated opus, opus verify (MINI-05 P1, store-layer). Took the plan's PRIMARY fix shape for all three (pinned by me — no persist field, version stays 29, no co-op impact; Dungeon Delve is solo turn-based). MINI-05: plain combat rooms paid ZERO gold (only elite added gold; enemy `rewards` field hardcoded 0/dead). Added pure `combatRoomGold(depth)=30+depth*5` in dungeon.ts (≈half resolveTreasure's 60+depth*10 base, matches resolveTreasure/merchantOffers depth-scaled-fn idiom); dungeonAdvance won-branch now pays it on combat/boss wins via `mergeReward(workingRun.floorReward, {gold})` — spreads workingRun so it preserves the earnedXp/HP-forward/trained-stat accumulation. Elite stays on its own if-branch (+40+depth*12), mutually exclusive → no double-pay, elite still richer at every depth (52 vs 35 at d1). BAL-13: dungeonDescend deducted NO energy (only entry via startDungeon did). Now chargeEnergy = depth>3 && !unlimitedEnergy → clamps energy to max(0,energy-1) + energySpentPatch(s,1), mirroring startDungeon; charge-if-available, NO hard block (won't strand a mid-run player), unlimited bypasses. DUNGEON_ENERGY_COST (entry, =3) untouched. MINI-30: flee kept 100% of gathered loot (finishRun keepFactor 1) → changed to 0.6 (scaleReward floors gold*0.6); lost branch's FLOOR_LOSS_KEEP=0.25 untouched. Verified opus PASS-WITH-NITS: NIT-1 (accepted) energySpentPatch records 1 spend even when energy already 0 — tiny ledger overcount, matches finding's ledger-consistency intent. NIT-2 (out-of-scope, logged below) pre-existing elite-branch earnedXp discard. Tests: combat win floorReward.gold===combatRoomGold(1)=35 + banked 20+35; flee banked 100→60; descend energy 5→4 charged past floor 3 & 5→5 under unlimitedEnergy/floor≤3 — all fail on old code. 179 tests pass (integration+dungeon), tsc clean. DISCOVERED (pre-existing, NOT fixed — out of scope): dungeonSlice.ts:249 elite win-branch `workingRun={...run,...}` discards earnedXp set at :245 → elite wins under-report earnings.xp['dungeon'] (telemetry only; actual stat XP granted separately via grantStatXp, no gameplay loss). Candidate for a Phase 7 sweep. |
| 2026-07-06 | 4.7 | BAL-12, BAL-25 | src/engine/mining.ts, src/engine/forest.ts, src/store/shared.ts, src/store/slices/miningSlice.ts, src/store/slices/forestSlice.ts, src/net/coop/session.ts, src/store/__tests__/store.integration.test.ts, src/engine/__tests__/mining.test.ts, src/engine/__tests__/forest.test.ts | Delegated opus, opus verify (store+net touched, co-op desync-critical). Design decisions PINNED by me. BAL-12: end-of-run bank paid 100% from ANY tile (making Forest's clearing-gated 0.8 stash + all safe tiles pointless). Now `commitMining`/`commitForest` read the player's current tile kind and scale the banked haul via `splitHaul(...).kept` (same mechanism as death) — full 1.0 only on a safe tile, else stash rate 0.8. Added `MINE_STASH_KEEP=0.8` mirroring existing `FOREST_STASH_KEEP=0.8`; pure `isMineSafeBankTile` (entrance-only) / `isForestSafeBankTile` (entrance+clearing) — tile-kind strings confirmed to exist in the unions & match the spawn cell. Graduated scale now death 0.5 < off-tile 0.8 < safe-tile 1.0. Death paths untouched (don't route through the predicate — dying on entrance still 0.5). BAL-25: beginMining/beginForest hardcoded floor/stage 1 despite persisted deepestMineFloor/deepestForestStage. Added pure `unlockedStartFloor`/`unlockedStartStage` = highest guardian boundary STRICTLY BELOW deepest (MINE_GUARDIAN_FLOORS [7,15], FOREST_GUARDIAN_STAGES [4,8]; deepest 6→1, 8→7, 16→15). SOLO-ONLY: added optional `startFloor`/`startStage` param defaulting to the computed solo value, threaded IDENTICALLY into generateMine/Forest + floorSeed + tombstone `floor===start` check (verifier confirmed no seed/generate mismatch). CO-OP PINNED: net/coop/session.ts beginRun passes explicit `1` → host+guest seed identical shared map; NO broadcast/message-shape/PROTOCOL_VERSION change (local positional arg only). NO persist field added, version 29. Verified opus PASS-WITH-NITS: NIT (accepted, logged) a tombstone recorded on a floor now below the unlocked start becomes unrecoverable since deeper solo starts skip it — small, needs a specific no-death-since sequence; candidate for a future clamp of tombstone recovery to >=start. Verifier also noted an 'unrelated status enum change' in session.ts not in the implementer's report — tests+tsc green and no protocol impact; DOUBLE-CHECK at check-4 whether it's stray scope or pre-existing. Tests: off-tile bank asserts exact 0.8× (+40 gold/+2 iron), safe-tile full (+50/+3), solo start floor 7 / co-op floor 1, + pure helper units — all fail on old code. 256 tests pass, tsc clean. |
| 2026-07-06 | 4.9 | BAL-09, MINI-25 | src/engine/progression.ts, src/store/shared.ts, src/store/useGameStore.ts, src/store/slices/coreSlice.ts, src/engine/arena.ts, src/engine/__tests__/{progression,arena}.test.ts, src/store/__tests__/{store.integration,persistMigration}.test.ts | Two independent sub-fixes. MINI-25 (delegated sonnet, transcription): Arena tallied EN at +1 per attack (parity with the real attack stat) → changed both attack sites (arenaMelee/arenaRanged) to +0.25 so commitArena's proportional split gives EN a modest trickle not ~50% of budget; confirmed commitArena's split has no integer assumption on statUsage (plain reduce + Math.round after division), so fractional weight flows through; 3 EN test assertions updated (1→0.25, 2 attacks→0.5). 48 arena tests pass. BAL-09 (DIRECT — persist migrate, version 29→30; verified fable PASS zero-issues): minigame trickle XP was allocated to stat POINTS at level-up with the same weight as deliberate habit effort, letting grinding steer allocation. Added `MINIGAME_XP_ALLOCATION_WEIGHT=0.5` (progression.ts) + new persisted Character fields `statXpTrickle`/`statXpTrickleAtLastLevel` (a sub-ledger of statXp). applyReward now double-books statXp into statXpTrickle when source ∈ {mine,forest,arena,tactics} (new TRICKLE_SOURCES set); habits/dungeon/trial/challenge/boss stay full-weight. applyLevelUp + previewNextGains compute delta = full − (1−0.5)·trickleDelta before allocateStatGains (kept in LOCKSTEP via the shared constant); applyLevelUp snapshots statXpTrickleAtLastLevel alongside statXpAtLastLevel (adjacent+unconditional). CRITICAL invariants verified: (1) leveling PACE unchanged — checkLevelUp still uses raw totalXp(statXp); statXp never reduced, discount only affects the allocation delta. (2) Aliasing closed — commitRun AND stashForest clone statXpTrickle (the only two applyReward paths with a trickle source); `{...undefined}`→`{}` so an unmigrated in-memory char can't crash; cloud pulls run migrate/merge first via persist.rehydrate. (3) trickle ⊆ statXp (same n written in same loop iter) → trickleDelta ≤ fullDelta → delta ≥ 0. Migration: v30 backfills both fields to emptyStatXP() (past XP = full-weight, no retroactive penalty); merge/withCharacterDefaults spreads freshCharacter() as a 2nd net; devSetLevel zeroes both. Tests: progression flip test (AG 300 habit beats DX 400 all-trickle — fails pre-fix), integration (mine commit records trickle==statXp delta; habit XP lands in statXp NOT trickle), persistMigration v30 zero-backfill, version-pin 29→30. 209 tests across the 4 suites pass, tsc clean. |
| 2026-07-06 | 4.10 | BAL-18, BAL-19, BAL-15, BAL-26 | src/store/shared.ts, src/content/weapons.ts, src/store/__tests__/store.integration.test.ts, docs/habits-rpg-improvement-plan3.md (deferred table) | DIRECT (open-design decisions to record). No scout needed for the decisions — I made all four; no verifier (content/constant/doc only, no logic). BAL-18 (code): MAX_ENERGY 50→15 (shared.ts) — energy is now a spend-pacing lever (~2-3 days), not an anti-bug ceiling; a lapsed player can't return to ~16-25 banked runs funded by zero habits. Docstring reframed. No migration/clamp-on-load (Option A friendly-trust — over-cap saves settle on next energy action; no test hardcodes 50, all import the constant). BAL-15 (code): iron_mace + short_bow shop price 120→55 (weapons.ts) — a convenience premium over crafting (~20g+materials), not the old ~100g decoy trap for players who haven't found the Forge; hunting_bow (no recipe) stays 170g. BAL-26 (code): short_bow staminaCost 2→1 (weapons.ts) — gives it a real niche as the cheap, stamina-efficient early bow so hunting_bow (bonus 5/range 5, cost 1) is a clear upgrade rather than a strict dominator on every axis; recorded as the accessible ranged stepping stone. BAL-19 (doc-only, NO code): level-up boss retries stay free/unlimited — energy-gating a mandatory progression gate is punitive and it's not farmable (one pendingLevelUp/level, nulled before reward); spec's unbuilt "Boss dungeon = 10 Energy" stays out of scope. Decisions recorded in plan3 "Still open / deferred" table (updated the Energy-decay row to Decided w/ the cap number; added a Boss-gate-retries Decided row). Test: buyWeapon('short_bow') gold assertion 80→145 (200-55). 169 integration tests pass, tsc clean, version 30 (unchanged by this item). |
| 2026-07-06 | 4.8 | MINI-08 (P1), MINI-23, MINI-22, BAL-10 | src/engine/hexBattle.ts, src/store/slices/tacticsSlice.ts, src/store/shared.ts, src/views/TacticsView.tsx, src/components/tactics/TacticsOverlay.tsx, src/engine/__tests__/hexBattle.test.ts, src/store/__tests__/store.integration.test.ts | Delegated opus, opus verify (MINI-08 P1). All design decisions PINNED by me. MINI-08: beginTactics gained optional `chosenTier?` clamped `Math.max(4, Math.min(level, chosenTier ?? level))` (tacticsSlice.ts:58); ephemeral useState tier picker in TacticsView (tiers 4..level, default level, mirrors SIZE picker, NOT persisted, no settings field) passed to both begin call sites. beginTacticsCoop UNTOUCHED — solo-only choice (same principle as 4.7/4.9). MINI-22: tacticsReward baseGold now = round(40·(1+tier·0.15)·(1+0.15·sizeBonus)); extracted `tacticsSizeBonus(radius)` shared with generateSkirmish (old inline `radius-3+floor((radius-3)/2)` fully replaced, single source); objective ×1.6 composes on top (win-only); tier-8 potion threshold stays tier-based. MINI-23: non-won pays round(baseGold·tacticsDamageFraction) WITHOUT ×1.6; tooltip (TacticsOverlay.tsx:789) made honest. DEVIATION FROM IMPLEMENTER→improved in main session: implementer's first fraction was survivors-only (checkOutcome removes slain enemies → kills uncredited, tooltip over-promised). I added optional `enemyForceMaxHp` to HexBattleState (captured as-spawned in generateSkirmish, preserved by `...s` clone) as a frozen denominator so fraction = (enemyForceMaxHp − Σ standing hp)/enemyForceMaxHp — now credits KILLS, not just chip damage; survivors-only fallback when field absent (legacy in-flight runs); NO persist version bump (optional field, version stays 30), no migration. BAL-10: win awards `materials {cloth_roll:q, bronze_bar:q}`, q=1+floor(tier/4) (both scarce in other modes — Tactics becomes a distinct source); MANDATORY commitTactics cloneMaterials false→true (shared.ts:1366) — else applyReward's `state.materials[k]+=n` aliases shared state (arena's cloneMaterials:false untouched). Verified opus PASS all 4 findings, zero issues; tests honest (radius6→112, aliasing asserts matsBefore stays {}, oneKilled→35 fails survivors-only, clamp below/above/valid/default). 250 tests (hexBattle+integration) pass, tsc clean. |
| 2026-07-06 | check 4 (Phase 4 close-out) | — | docs/habits-rpg-improvement-plan3.md (heading date-stamp + 2 deferred rows) | Full suite 1465 tests / 59 files pass, tsc exit 0. game-design-auditor numeric-balance re-check (opus): ALL 10 Phase-4 changes verified present in engine code (zero plan-vs-code drift). Acceptance: crit-1 (majority-habit L10–L20) MET for realistic play (56–70% habit share) — residual BAL-22 trivial-breadth exploit deferred; crit-3 (no strict dominator) MET (old Arena 500g/win P1 gone; no mode tops both gold/min & xp/min; Arena a farming default only); crit-2 (≥500g target) NOT MET by pure-gold (max item 220g) — Orion ACCEPTED the gap (mithril toolkit's total material+gold acquisition ≈ ≥500g-worth), Phase 4 date-stamped, ≥500g pure-gold sink + BAL-22 energy cap logged as deferred design calls. Phase 4 NOT in {1,2,7} → no code-health-auditor. |
| 2026-07-06 | 5.1 | BAL-02 (P1), BAL-06, BAL-14 | src/engine/bosses.ts, src/engine/enemies.ts, src/engine/stats.ts, src/engine/arena.ts, src/engine/__tests__/balance.test.ts | Delegated opus, opus verify (BAL-02 P1). Design PINNED by me for the 3 named gates; implementer chose L15/25/30 per my rotation rule. Damage can only carry {ST,DX,WI} (weapon attackStat ∈ {ST,DX}; damage spells always WI) → every CH/AG/EN/KN/HP weakTo/resistTo entry was DEAD (affinity mult weak ×1.25 / resist ×0.6). BAL-06: constrained ALL affinity content ⊆ {ST,DX,WI} across bosses (7 named + all phases + generic fallback), enemies (4 dirty CH entries: ghoul weakTo, dire_wolf/frost_troll/ice_wolf resistTo → cleaned), biomes.ts (re-verified already clean). Added `DAMAGE_STATS=['ST','DX','WI']` (stats.ts) as the lint's source of truth. BAL-02 (rig fix): drill_rex L8 weak['CH','AG']/resist['ST'] → weak['ST','DX']/resist['WI']; comfort_blob L12 weak['AG']/resist['ST','EN'] → weak['WI']/resist[]; burnout_golem L20 p1 weak['WI','EN']→['ST','DX'], p2 weak['WI'] kept. Implementer's L15 wraith→weak['WI'], L25 mirror_demon p1→['ST','DX']/p2→['WI'], L30 clockwork_tyrant p1→['ST','DX']/p2→['WI']. Rotation: no mandatory gate resists ST or DX (only WI resisted, at L8); every gate winnable at-level by pure ST, first attempt, no pity — verifier confirmed no ST-unwinnable gate. BAL-14: bossForLevel generic attack 4+round(0.7t) → 4+round(0.7·min(t,30)+0.25·max(0,t−30)); continuous at t=30 (=25), t=40 32→28, t=50 39→30, keeps late gates winnable vs STAT_CAP-25+6 ceiling. Behavior-preserving genericBossReward extraction wired at arena.ts:630 (arena.test.ts covers it). Tests: 7 new in balance.test.ts (ST-build gate wins L8/L12/L20 via simulate+fixed(0.5); direct-affinity guard; content-lint ⊆ DAMAGE_STATS over bosses+enemies+biomes; BAL-14 curve values) — all fail on old data, pass after. Verified opus PASS-WITH-NITS: NIT-1 (deferred to Phase 7 test-coverage) content-lint doesn't yet iterate mining.ts/forest.ts node affinities (already clean, no live bug); NIT-2 genericBossReward refactor exceeds stated 4-file surface (behavior-preserving, flagged for transparency). 88 tests across 5 combat/balance suites pass, tsc clean. |
| 2026-07-06 | (side-fix during 5.1) | — | src/store/__tests__/store.integration.test.ts | DISCOVERED + FIXED (pre-existing, unrelated to 5.1): the `completeHabit > records energy earned in energyLog for today` test computed its expected key via `new Date().toISOString().slice(0,10)` (UTC) while the store keys energyLog via the engine seam `toISODate()` (LOCAL date — date.ts:58-63 uses getFullYear/getMonth/getDate). They diverge past the local/UTC midnight boundary → the test passed at check-4 (16:03 local) but false-failed at ~22:00 local (UTC already next day). The store is correct (a habit tracker's day = the player's local day); the test was wrong. Changed line 201 to `const today = toISODate();` (already imported, used at :1217). Now deterministic across all times of day — unblocks every future `check <phase>` full-suite run. Not a plan item (no marker flip). |
| 2026-07-07 | 5.2 | MINI-03 (P1) | src/engine/bosses.ts, src/engine/__tests__/{bosses(new),combat.moves,balance}.test.ts | Delegated opus, opus verify (MINI-03 P1). DATA-ONLY (verified no combat.ts/hexBattle.ts/arena.ts/EnemyMove-type change): combat engine already runs EnemyMove[] for trash mobs; createBattle synthesizes BossDef.moveset into phase[0] + reads per-BossPhase.moveset; pickEnemyMove (weighted RNG) + executeEnemyMove run them. Authored a 3-move pool per named boss / per phase (7 bosses, multi-phase get per-phase sets) + a shared TRIAL_GUARDIAN_MOVESET wired into bossForLevel's generic fallback. Each set = draugr_mage idiom: plain attack (weight 2-3) + one damaging special (heavy/multi, weight 1) + one utility (guard/inflict/enrage, weight 1). CONSTRAINT held: weighted-avg per-turn damage AT/UNDER each boss's flat `attack` (verifier recomputed: rex 10.7≤11, blob 9.5≤10, wraith 11.4≤12, golem p0 17.55≤18/p1 20.35≤22) so 5.1's tuned gates don't re-break. Anti-spam move on every named boss (guard/weaken/blind/enrage) per Phase-5 acceptance. Only valid kinds (7) + inflictKeys (burn/poison/weaken/blind/freeze). DEVIATION (verifier-approved): balance.test.ts simulate() shared helper fixed(0.5)→fixed(0.49) so weighted move-selection deterministically lands on each boss's plain attack (each listed first, weight ≥ 50% of total) — reproduces 5.1's flat-attack tuning baseline; at exactly 0.5 `r-=2` hits 0 (not <0) and falls through to heavy, so 0.49 is load-bearing. SAFE: dodge cap 0.4 so no dodge/miss flip; variance 0.997× vs 1.0× (0.3% delta); 5.1's BAL-02 gate tests still fail-on-old (affinity resist is independent of move-selection). Gate sim is CONSERVATIVE not optimistic (real full-moveset avg damage ≤ plain-attack sim since guard turns deal 0) — verifier confirmed winning sim ⇒ winning average real fight. Updated the stale combat.moves.test "no moveset" test → asserts presence. New bosses.test.ts: every named boss+phase+Guardian has 3 valid moves. 34 tests pass (3 suites), tsc clean. Verified opus PASS, zero blockers. LOGGED for future balance pass (not this item, no L30 gate sim exists): clockwork_tyrant L30 phase-1 enrage compounds bossAttack permanently → real long-fight burst can exceed the flat-attack baseline. |
| 2026-07-07 | 5.3 | MINI-04 (P1), MINI-29 | src/content/biomes.ts, src/engine/dungeon.ts, src/engine/combat.ts, src/store/shared.ts, src/store/slices/coreSlice.ts, src/store/slices/dungeonSlice.ts, src/engine/__tests__/{combat.phases,balance}.test.ts, src/store/__tests__/store.integration.test.ts | DIRECT (persist-touching + new store field + cross-file). The finding's fix is a package; all four parts landed. (1) HP CUT: all three biome bosses' baseHp + every phase hp cut ×0.65 round (bone_tyrant 110→72/90→59; vinewood 130→85/110→72; frost_warden 115→75/95→62/85→55); attack unchanged. (2) BOSS GOLD: new `bossRoomGold(depth)=100+depth*50` (dungeon.ts, vs combatRoomGold 30+depth*5); dungeonSlice boss-win now pays it via its own branch (elite/plain-combat branches untouched, mutually exclusive → no double-pay). (3) RELIEF THREADING: new persisted `dungeonBossLosses: Record<string,number>` (keyed by boss id `${base}_d${depth}`) on GameState/CoreSlice + `{}` init in createCoreSlice & resetGame; enterRoom's boss branch computes `boss` then passes `lossesBefore: state.dungeonBossLosses[boss.id] ?? 0` into createBattle so anti-frustration relief (combat.ts, ≥3 losses → up to −40% HP) finally fires for dungeon bosses; dungeonSlice increments the tally on a boss LOSS (not flee/non-boss) and resets it to 0 on a genuine boss WIN. (4) MULTI-PHASE REWARD (MINI-29): new required `hpDefeated: number` on BattleState (init 0); resolveBossDown banks `s.hpDefeated += s.bossMaxHp` as its FIRST line (before applyPhase overwrites bossMaxHp for the next form, before the victory branch) → sums every cleared phase; dungeonSlice's three former `b.bossMaxHp` reads (combatXpForWin, dungeonCombatStatXp, damageDealt) now use `totalHp = b.hpDefeated ?? b.bossMaxHp`. DEVIATION from the pinned plan: **NO persist version bump (stays v30)** — dungeonBossLosses backfills exactly like the pre-existing bossLosses field (merge `{...current,...p}` + coreSlice `{}` initializer → missing key rehydrates to `{}` not undefined; no read runs before init/merge since both reads require an active dungeon run), and `hpDefeated ?? bossMaxHp` handles a legacy in-flight battle; a bump would be MORE destructive (migrate nulls dungeon). Single-phase/plain-combat/elite: resolveBossDown adds bossMaxHp exactly once → totalHp==bossMaxHp, behavior unchanged. Verified opus PASS, zero issues (traced merge backfill, loss-key consistency write-vs-enterRoom-read, no twin drift — level-up gate pays fixed boss.rewards not HP-derived; arena has its own ArenaState). Tests: hpDefeated 0→10→20 across twoPhase (combat.phases); a prepared L7 iron_mace relief-assisted build beats bossFor(catacombs,5,7) — counterfactual-confirmed non-vacuous (SAME build dies round 14 at old inflated HP, wins round 12/47 HP at cut HP); simulate() gained a lossesBefore param; store: boss-win floorReward.gold===bossRoomGold(1) + damageDealt===120 (from hpDefeated not 60) + dungeonBossLosses reset to 0, boss-loss increments to 1; the `satisfies BattleState` literal gained hpDefeated:0. 215 tests across 6 touched suites pass, tsc clean. |
| 2026-07-07 | 5.4 | MINI-06 (P1), MINI-09 (P1), MINI-20, MINI-21 | src/engine/arena.ts, src/components/ArenaOverlay.tsx, src/engine/hexBattle.ts, src/content/mining.ts, src/content/forest.ts, src/engine/mining.ts, src/engine/forest.ts, src/engine/crawl.ts, src/engine/__tests__/{arena,hexBattle,mining,forest,crawl}.test.ts | Split into 3 file-disjoint chunks, 3 fix-implementers (opus) in parallel + 2 P1 verifiers. **A — Arena (MINI-06):** speed cap 1.2→1.6 in arenaSpeedFactor 'auto' band (boss stops slowing to a crawl at high level); new 'charge' TelegraphKind {windupMs:720,dmgMult:1.1}; chooseKind biases charge when dist>3 (gap-closer vs a fleeing player); telegraphTiles lays a charge lane down the boss→player line; resolveTelegraphs 'charge' branch → resolveCharge() dashes bossPos along the lane, damage only on adjacency-connect (a dodged charge whiffs). ArenaOverlay 'RUSH' label. Verified opus PASS-WITH-NITS (accepted: residual tangential-strafe can still out-run a charge that aims down the CURRENT retreat line — a lesser dodge skill, not the damage-free kite the finding names; boss auto-paths behind the player so the lane tracks). **B — Tactics (MINI-09):** charger lunge — new optional `turnsOutOfReach?: number` on EnemyUnit (init 0, `?? 0`-read, rides the wholesale HexBattleState broadcast so no PROTOCOL bump); after 2 turns stuck out of reach a chasing enemy gets a one-off doubled move budget. Verifier 1st pass PASS-WITH-NITS found a medium-board gap (radius-4: a max-AG bow player's top move is 6 and a plain 2×moveTiles=6 lunge stalled at distance 2 forever); applied the exact fix — budget `2*moveTiles+1` (strictly out-closes the fastest retreat on every board) + dropped the `|| lunge` from the counter reset (a whiffed lunge keeps pressing every turn until it connects, not resetting) + extended `chasing` to flanker (the other melee close-to-engage archetype; holder/kiter stay excluded). 2nd-pass verify PASS-WITH-NITS (only a stale docstring, fixed). **C+D — Crawlers (MINI-20/21):** new late-band sub-300ms enemies — mine `cinder_wisp` (floorMin 20, magma band, hp 30, touchDamage 14, moveCadenceMs 250, bounty [55,90]) + forest `amber_stalker` (stageMin 12, ancient band, hp 26, touchDamage 13, moveCadenceMs 250, bounty [40,60]); uncapped monster count (mine `min(mFloor.length, 2+floor·0.6)`, forest `min(trailCells.length, 5+stage)`) and added `crawl.ts` `lateDepthDamageScale(depthPastLastBand)=min(2, 1+0.04·max(0,past))` applied at both contact-damage reads per crawler (main + coopClientStep, twins kept in lockstep). DEVIATIONS: (A) no full charge homing on a strafing player — accepted as scoped; (B) NO persist/PROTOCOL bump (field backfills via `?? 0`, broadcast is wholesale), lunge extended beyond the plan's "charger" to flanker per verifier nit; (C+D) count uncap bounded by available floor/trail cells (not literally infinite — logged as intentional, no soft-lock). Tests: arena 51 pass; hexBattle 81 pass (medium-board budget regression is non-vacuous — fails on old budget 6 landing at dist 2, passes at 7→dist 1; + flanker-lunges / holder-kiter-never scope test); crawlers 95 pass (new-enemy band membership + lateDepthDamageScale monotonic). Full touched-suite + tsc clean. |
| 2026-07-07 | 5.5 | MINI-07 (P1), MINI-26, MINI-37a | src/engine/arena.ts, src/engine/__tests__/arena.test.ts | DIRECT (persist-adjacent: additive field on the persisted+rehydrated ArenaState — but no version/migrate bump; single-file + tests). **MINI-07 (ice_rune stunlock):** new `bossFreezeImmuneUntilMs: number` on ArenaState (init 0 at all 3 reset sites — createArena, rebaseArenaRun, boss-phase-advance; `?? 0`-guarded read); new `FREEZE_IMMUNITY_MS = 6000`. In triggerRunes/fireRune ice-boss branch a freeze lands only `if (now >= bossFreezeImmuneUntilMs)` and stamps immunity `= now + FREEZE_DURATION_MS + FREEZE_IMMUNITY_MS` on landing → one 3s freeze per ≥9s cycle (boss active 6/9s), which 1.8 MP/s regen (7 MP/cast) can't sustain → stunlock dead. Verifier confirmed triggerRunes is the ONLY nonzero writer of bossFrozenUntilMs and bossThink the only reader (the enemyStatuses 'freeze' entry is cosmetic, never read by AI); a rune stepped on during immunity still chips but can't re-lock. **MINI-26 (rune/ring ward bypass):** rune boss hit now `Math.max(1, rune.power - s.bossWard)` (minions unchanged); tickRingOfFire boss branch subtracts `s.bossWard` (minion branch unchanged) — both floor at 1, inside the existing hit-CD gate. Equivalent to the direct-spell path (`Math.max(1, round(dmg) - ward)`) which was the mirror. **MINI-37a (bolt affinity at impact):** arenaRanged now rolls affinity-free (`[]`,`[]`); stepProjectiles player-projectile branch applies boss DX weak/resist ×1.25/×0.6 + bossDefense ONLY when `here.kind==='boss'`, minions take raw `p.dealt` — mirrors melee's isBoss gate. Minor rounding-order delta vs melee (round→×affinity→round vs ×affinity→round) is ±1 cosmetic on boss hits only. DEVIATIONS/scope: (a) NO persist version bump (additive optional-safe scalar; rebaseArenaRun always runs on rehydrate so the field is numeric on every live path). (b) DoT rider NOT ward-reduced (burn/poison magnitude from pre-ward rune.power) — verifier-flagged NIT, left as-is: it's not the cited mechanism (direct hit) and is consistent with direct-spell riders using fixed magnitudes; out of scope for 5.5. (c) rune roll's empty affinity arrays at cast (`[]`,`[]`) left as-is — same target-unknown-at-cast root as MINI-37a but not in the 5.5 scope. Twin/layer: NO co-op path for Arena (src/net broadcasts Mine/Forest/Tactics only), engine purity held, no mining/forest twin. Tests: 5 new (MINI-07 immunity blocks re-freeze + expires→re-freezes; MINI-26 fire_rune & ring ward-reduced by exactly 4; MINI-37a bolt=8 raw on minion vs 10 DX-weak on boss) — all non-vacuous (pre-fix: 2nd freeze moves window, ward diff 0, minion inherits 10). 56 arena tests pass, tsc clean. Verified opus PASS-WITH-NITS (1 out-of-scope NIT). |
| 2026-07-07 | 5.6 | MINI-17, MINI-18 | src/engine/crawl.ts, src/engine/mining.ts, src/hooks/useMiningLoop.ts, src/hooks/useForestLoop.ts, src/components/mining/MineControls.tsx, src/components/forest/ForestControls.tsx, src/engine/__tests__/{mining,crawlers-phase1}.test.ts | DIRECT (cross-file: engine + hooks + components, with a co-op damage-lockstep requirement). **MINI-17.1 (mine frozen parity):** the mine's contact-damage finder ignored `frozenUntilMs`, so a monster staggered by a charge still bit — the forest already gates on frozen. Added `!(m.frozenUntilMs && nowMs < m.frozenUntilMs) && adjacent(...)` to BOTH mine sites (main-loop stepMonsters ~1333 AND coopClientStep guest twin ~1416), mirroring forest.ts:1490/1560. This is what gives the charge's 500ms STAGGER_MS defensive value. Falsy short-circuit verified safe: never-frozen (undefined) → `!(undefined)` → still bites. **MINI-17.2 (charge DPS):** CHARGE_DAMAGE_MULT 1.75→2.25 in crawl.ts (a 2-interval hold now out-trades 1.0×/interval mashing instead of being DPS-negative). CRITICAL lockstep the scout caught: two hooks hardcoded the multiplier as a bare `* 1.75` literal in the co-op GUEST charged-attack payload (useMiningLoop.ts:168, useForestLoop.ts:169) — left un-bumped they'd desync guest-sent vs host-computed charged damage; both now import & use CHARGE_DAMAGE_MULT. Host paths (mining.ts:929/964, forest.ts:1038/1074) already use the constant; co-op reducer forwards wire `dmg` unchanged → all paths resolve to 2.25 (verifier grep-confirmed no residual 1.75 in engine/hooks/store/net/content). **MINI-18 (touch charge):** touch Act buttons fired only onPointerDown, so `swing()/act()` set `spaceDownAt` once and only keyboard keyup ever cleared it → first tap = one phantom 480ms charged swing, then charge stuck forever. Added a `releaseCharge()` method (nulls spaceDownAt+chargeConsumed, mirrors keyup) to both MiningControls/ForestControlsApi interfaces + returned APIs; wired onPointerUp/onPointerLeave/onPointerCancel → releaseCharge() on both Act buttons. A quick tap now fires a normal swing; a deliberate hold charges. DEVIATIONS/NITs (both accepted): (a) stale `1.75` citations remain in docs/*-minigame-analysis.md — dated pre-audit snapshots, not the plan/log, left per the 5.1 NIT convention; (b) onPointerLeave cancels an in-progress charge if a MOUSE pointer drifts off the button edge mid-hold — harmless for touch (implicit pointer capture keeps events on target), which is the fix's audience. COVERAGE GAP (non-blocking): touch hooks have no jsdom/RAF harness so the releaseCharge wiring is manual-test-only; both engine halves are covered. Tests: new mining.test.ts frozen-contact test (hp 50 while frozen, 46 once thawed — non-vacuous, pre-fix dealt 4); crawlers-phase1 CHARGE_DAMAGE_MULT≥2.25 assertion (was `>1` sanity) + two durability expectations updated to the 2.25 math (rock 5-3=2; chop dur 7<9). 128 crawler-suite tests pass, tsc clean. Verified opus PASS-WITH-NITS (2 accepted NITs). Manual playtest for Orion: on a phone, tap-and-hold the Mine/Act button — a held charge should now fire a heavy swing and a quick tap a normal one (pre-fix: every tap was a stuck phantom charge). |
| 2026-07-07 | 5.7 | BAL-07 (P2), BAL-08 (P2) | src/engine/combat.ts, src/engine/arena.ts, src/engine/hexBattle.ts, src/engine/mining.ts, src/engine/forest.ts, src/store/slices/dungeonSlice.ts, src/components/dungeon/ShrineRoom.tsx, src/content/spells.ts, src/content/encounters.ts, src/content/biomes.ts, src/engine/__tests__/{combat,hexBattle,encounters}.test.ts, src/store/__tests__/store.integration.test.ts | DIRECT (touches store slice + cross-file; unified 3 divergent inline copies). **BAL-07 (CH honesty):** (1) new generic `illusionBoost<T extends {turns;magnitude}>(status, illusionPower)` in combat.ts — `turns += floor(CH/4)` (was floor(CH/8), so most CH points now move the needle) + `magnitude += floor(CH/6)*0.05`. DEVIATION from the finding's literal `+floor(CH/6)`: weaken.magnitude is a FRACTION (0.4=40% cut per spells.ts:28), so a flat +floor(CH/6) is dimensionally broken (0.4+4=4.4); *0.05 keeps clean 0.45/0.5/0.55/0.6 steps. Wired into all THREE casters that had their own inline copy: combat.ts:496, arena.ts:915, hexBattle.ts:896. blind's magnitude stays inert by design (blind's CH lever is duration; miss chance is hardcoded rng()<0.4). (2) The two crawler illusion casts (mining.ts:1141, forest.ts:1238) bumped /8→/4 for slope-consistency (verifier SHOULD-FIX — same "CH barely moves" complaint surviving in 2/5 combat systems); their magnitude is DoT dmg/tick not the weaken fraction, so left untouched (durationMs shape can't take illusionBoost verbatim — divisor change only). (3) shrine pray now `power = statLevels.WI` (dungeonSlice.ts:415, was max(WI,CH) which made CH a redundant WI copy); ShrineRoom.tsx:14 display mirrors it. **BAL-08 (push/blink honesty):** hexBattle push distance `2+floor(CH/8)` (:845), blink radius `2+floor(KN/8)` (:532) — the tooltips already implied scaling; spells.ts:110/119 reworded to honest "farther with Charisma/Knowledge". weakenFactor clamped `max(0,1-mag)` (deepened weaken can exceed mag 1). **CH content:** new `tense_standoff` encounter (encounters.ts:25) with 5 CH checks + 1 DX, registered in all 3 biomes (catacombs/ruins/frozen). NO persist/protocol bump (pure engine/content/display + additive helper). Twin check: 3 illusion casters unified, 2 crawler casters slope-matched — verifier confirmed no other illusionPower-boost site missed; magnitude fraction never reaches burn/poison (illusion else-branch only carries blind/weaken). Tests: illusionBoost units (CH0 no-op / dur floor(CH/4) / mag floor(CH/6)*0.05); push CH0=2·CH16=4 tiles + blink KN0=2·KN16=4 (non-vacuous — 2 base fails); tense_standoff exactly-5-CH + high-CH pass + biome-registration loop; shrine WI0/CH30 roll 0.5→curse (non-vacuous — old max(WI,CH) chance 0.95 would have blessed). 289 targeted + 128 crawler tests pass, tsc clean. Verified opus PASS-WITH-NITS (the 1 SHOULD-FIX crawler slope now applied; all other items confirmed non-vacuous). |
| 2026-07-07 | 5.8 | MINI-27 (P2), MINI-28 (P2), MINI-24 (P2), MINI-39 (P3) | src/store/shared.ts, src/store/slices/dungeonSlice.ts, src/engine/encounters.ts, src/engine/combat.ts, src/engine/hexBattle.ts, src/views/DungeonView.tsx, src/components/dungeon/ShrineRoom.tsx, src/components/combat/BattleScene.tsx, src/engine/__tests__/{encounters,combat,hexBattle}.test.ts, src/store/__tests__/store.integration.test.ts | DIRECT (bundled row; store+net+persist-adjacent). **MINI-27 (relics reach checks):** new exported `runStatBonuses(state)` in shared.ts folds gear.statBonuses + run relics (aggregateRelics) + `dungeon.runBuff` + active lowHp stat-triggers into one bonus map; `fighterFor` REFACTORED to consume it (verified line-equivalent to the old inline sum — stat bonuses via helper, defense/ward/maxHp still accumulated separately, no double-count). Encounter choose now passes `runStatBonuses(s)` (was `gearBonuses(s).statBonuses`); shrine pray power now `statLevels.WI + (runStatBonuses(s).WI ?? 0)` (was raw WI from 5.7). UI mirrors honest: DungeonView odds + ShrineRoom odds read `runStatBonuses(useGameStore.getState())` imperatively (a fresh object from a plain selector loops under useSyncExternalStore; both are unmemoized children of the dungeon-subscribed parent so they re-render on any run change). **MINI-28 (depth scaling):** `chooseEncounter` got appended optional `depth=0`; check threshold now `(difficulty ?? 5) + encounterDepthTier(depth)`, gold `×(1 + tier*0.15)`; new exported `encounterDepthTier(depth)=floor(max(0,depth)/3)` (one difficulty pt / 3 floors, shared by resolver + UI so odds never lie); slice XP grant `10 + tier*2`. Appended param safe for the 6-arg test callers; `gateCtx.depth` and new `depth` are both `run.depth`. **MINI-39 (owned-weapon reroll + affinity chip):** reroll in `enterRoom`'s treasure branch (NOT pure resolveTreasure) — owned weapons filtered out of a fresh loot object, each adds `30+5*depth` gold; applyReward's dedupe stays a harmless fallback. New OPTIONAL `affinityRevealed?: boolean` on BattleState set true on the first attack/damage-spell hit tagged weak|resist (combat.ts:438,482 — the two player-visible tag sites; rune path shows no tag so no reveal, consistent); BattleScene pins weak (stat-colored ▲) / resist (▼) chips gated on it. **MINI-24 (beacon auto-complete):** new OPTIONAL `beaconBroken?: boolean` on TacticsObjective set on any end-of-turn beacon breach; checkOutcome win path grants the objective `if (!obj.complete && !obj.beaconBroken)` → a never-contested decisive board-clear completes it (clearing IS holding), a contested-then-cleared beacon does not, a legit full streak survives via the `!complete` guard. Optional AI beacon-attraction term (finding's "optionally") SKIPPED per scope. DEVIATIONS/persist/wire: NO version bump — both new fields are additive optional booleans on already-persisted (BattleState) / already-broadcast-wholesale (HexBattleState/TacticsObjective) objects; legacy saves read falsy, co-op reducer carries beaconBroken with no shape assumption broken. SIDE-FIX (logged, not scope creep): the pre-existing `combat.test.ts > defense mitigation` test was latently flaky — it built two battles with the DEFAULT rng, so boss opening intents came from consecutive global Math.random values; this item's Math.random-consumption shift exposed it as a failure only when run alongside encounters.test.ts. Fixed by passing `fixed(0.5)` as createBattle's rng so both foes telegraph the same move (verifier confirmed legitimate, not masking a regression). Tests: encounterDepthTier + shallow-passes/deep-fails + gold 30→53; affinity flip on forced-weak vs neutral hit; beacon auto-complete-when-clean vs no-freebie-when-broken; shrine +20 WI runBuff carries a 0.5 roll to boon (raw WI0 would curse); treasure owned iron_mace rerolls to gold 135 (=90 base+45 reroll at depth 3). All counterfactual-confirmed non-vacuous. 313 tests across 5 touched suites pass together (no cross-file pollution), tsc clean. Verified opus PASS — zero blockers, zero nits. |
| 2026-07-07 | 5.9 | BAL-23 (P3), BAL-24 (P2), MINI-31 (P2) | src/engine/hexBattle.ts, src/hooks/useArenaLoop.ts, src/content/boons.ts, src/content/encounters.ts, src/engine/mining.ts, src/components/mining/MineRunOverlay.tsx, src/engine/__tests__/{hexBattle,arena,mining,encounters,crawl-boons}.test.ts | DIRECT (small stat-parity cluster; content/component/engine only — no store/net/persist, so tests+typecheck, no verifier). **BAL-23 (AG reads in Arena/Tactics):** Tactics `moveTilesFor` cap 6→7 via `min(7, 2+floor(AG/4))` (AG 20 ceiling — a max-AG hex mover now genuinely out-ranges a sluggish build). Arena's flat 150ms step now tapers: new exported `arenaMoveIntervalFor(ag)=max(90, 150-ag*3)` (90ms floor at AG 20) wired into useArenaLoop's rAF move gate reading `character.statLevels.AG` — Arena's first real Agility payoff. **BAL-24 (HP under-checked):** added 4 `stat:'HP'` "endure/toughness" choices to existing hazard nodes (sealed_door.door, bone_pit.sinking, collapsing_bridge.falling, frozen_chasm.slipped) using the neighboring EncounterChoice shape (no new node keys → skips content-lint); HP labelled "(Hit Points)" per gear.ts convention. **MINI-31 (lantern + tombstone compass):** lantern boon `game:'forest'`→`'both'` (mine already reads sightBonus — fog had no counterplay); new pure `findTombstone(state):{r,c}|null` scan helper next to placeTombstone in mining.ts; MineRunOverlay factored the shaft-arrow IIFE into a shared `compassTo(target)` 8-way helper and added a rose 🪦 tombstone-direction badge (off-screen only), reusing it. DEVIATIONS: none material — HP checks kept parallel to neighbors (success = clean, fail = hpOnFail damage), no guaranteed-chip since EncounterChoice has no success-damage field. Tests: moveTilesFor caps at 7 (AG20/30); arenaMoveIntervalFor taper 150→90 floor + monotonic; crawl-boons lantern now offered in both crawlers (dropped from mine-exclusion set); 4-nodes-each-have-HP-check + high-HP hero clears bone_pit.sinking on toughness; findTombstone null-then-locates + pure-scan (source state unmutated). 255 tests across 5 touched suites pass, tsc clean. |
| 2026-07-07 | 6.1 | MINI-10 (P1) | src/engine/trials/lastStand.ts, src/components/trials/games/LastStand.tsx, src/engine/trials/__tests__/trials.test.ts | DIRECT (engine + component; no store/net/persist). Anti-mash for the Last Stand (HP) trial, which was 3★-able by blind key-mashing (every whiff was free, keydown had no `e.repeat` guard). **Engine:** new `MASH_LOCKOUT_MS = 200` + pure `resolveBlockGate(lockoutUntil, el, matched) → {accept, lockoutUntil}`: a whiff starts a non-extending 200ms lockout that also rejects the *next* would-be-matched block (the teeth — mashing can no longer catch every lane at spawn); a clean match sets no lockout so disciplined play never trips it. **Component (LastStand.tsx):** `lockoutUntilRef`, gate call inside `block()` (covers BOTH the keydown path and the on-screen Button onClick mash vector — the gate lives in the shared block() body); `if (e.repeat) return` on the keydown handler (matches ArmoryBreak.tsx:109). `if (!gate.accept || !target) return` — `!target` is a redundant TS narrowing (implied by !accept). Tests: 5 new resolveBlockGate cases (matched-when-open accepts; whiff rejects+opens window; would-be-match during lockout still rejected [non-vacuous teeth]; repeat-during-window doesn't extend; reopens at deadline). 165 trials tests pass, tsc clean. Verified opus PASS-WITH-NITS. **CARRY-FORWARD (SHOULD-FIX → 6.3):** the 200ms lockout degrades but doesn't alone guarantee sub-3★ because `block()` still accepts & `reactionSpeed` still scores across the full 1400ms `SPAWN_AHEAD_MS` window (masher mean reaction ≈0.75-0.78, right on the 0.75 3★ line). Item 6.3 (MINI-12, "HP → the dormant `blockWindowForWave`") MUST wire `blockWindowForWave(a.wave)` into BOTH the accept filter (LastStand.tsx:187) AND reactionSpeed's denominator (lastStand.ts:52), not merely define an HP window — that's what gives this lockout its teeth and closes the HP trial's mash-3★. `check 6` acceptance for Last Stand depends on 6.1+6.3 together. NIT (known jsdom gap): component ref/gate/e.repeat wiring is unharnessed — engine test faithfully models the logic but can't catch a future wiring regression. |
| 2026-07-07 | 6.2 | MINI-11 (P1) | src/store/shared.ts, src/store/slices/trialsSlice.ts, src/store/slices/coreSlice.ts, src/store/useGameStore.ts, src/components/trials/TrialModal.tsx, src/components/trials/games/AncientLibrary.tsx, src/components/trials/games/SpiritGrove.tsx, src/engine/trials/__tests__/{trials,spiritGrove}.test.ts, src/store/__tests__/store.integration.test.ts | DIRECT (persist v30→v31 + cross-file). Closes the trial-retry replay exploit. **Store/persist:** new persisted scalar `trialAttemptNonce` (GameState in shared.ts + TrialsSlice) + `startTrialAttempt()` action that increments it; persist v31 (migrate `?? 0`, merge via default `...p` scalar spread, resetGame reinit 0). Persisted (not transient) so a mid-exploit refresh can't reset it to a predictable value. **Seeding:** `startTrialAttempt()` fires on the "Begin Trial" click (BEFORE the game mounts), so each run gets a fresh nonce; TrialModal threads `attemptNonce` to AncientLibrary + SpiritGrove only. Library `masterSeq = generateSequence(seededRng(dailySeed(iso) ^ attemptNonce))` (was `dailySeed(iso)` alone — identical across reopens → transcribe exploit, now dead). Grove drafts with `seededRng(dailySeed(iso) ^ attemptNonce)` (was raw Math.random). Abandon dialog reframed ("A fresh challenge awaits next time"). XOR is bijective on distinct nonces; dailySeed (~2×10⁷) « 2³¹ so seededRng's `>>>0` stays valid; undefined would ToInt32→0 (no NaN). **DEVIATION/scope (verifier PASS-WITH-NITS):** Library's stable-set transcribe exploit is fully closed; **Grove's reroll-for-a-known-draw is NOT** — the nonce advances every Begin, so reopening still yields a fresh set (net-neutral vs the old Math.random for the reroll vector). That residual is inherent to **free abandonment** (MINI-11's named root cause) and needs a cost-on-abandon to close — a larger design change left out of this seed-scoped item; the SpiritGrove comment now states this honestly rather than overclaiming. Impact bounded: difficulty mix is fixed 1E/2M/2H, so only a familiar-questions set can be fished, not a strictly easier one. Also NOT done: per-game "Run Again" buttons on the already-nondeterministic trials (Rooftop already has one) — the "framing everywhere" was satisfied at the modal level (Abandon reframe) + the two deterministic reseeds, which is the actual exploit surface. **Lost property (acknowledged):** Library's "same glyphs for all players per day" is sacrificed (nonce is per-account) — no leaderboard/co-op/net consumer of the sequence (grep-confirmed), documented in-comment. Tests: engine same-nonce→identical / different-nonce→different master sequence (Library, non-vacuous vs pre-fix identical); Grove prepareRounds same-nonce-stable / different-nonce-redrafts; store startTrialAttempt 0→1→2 + completeTrial leaves nonce untouched; persist-version assertion → 31. 200 engine-trials + 179 store tests pass, tsc clean. **CARRY-FORWARD:** full closure of Grove reroll (and the general free-abandonment root cause) is a candidate for 6.7 (gate honesty) or a dedicated decision — charge energy/gate on trial START rather than only on completion. |
| 2026-07-07 | 6.3 | MINI-12 (P2), MINI-10 carry-forward | src/engine/trials/lastStand.ts, src/engine/trials/armoryBreak.ts, src/engine/trials/rooftopChase.ts, src/hooks/useChaseLoop.ts, src/components/trials/games/{RooftopChase,ArmoryBreak,LastStand}.tsx, src/components/trials/TrialModal.tsx, src/engine/trials/__tests__/trials.test.ts | DELEGATE-opus (four disjoint engine areas converging on the shared TrialModal — landed as one coherent diff to avoid conflict; P2, no store/net/persist → tests+typecheck, no verifier). Wires each of three trials to read its namesake stat, adds the missing TrialModal stat boxes, and **absorbs the 6.1 carry-forward** (gives the MINI-10 anti-mash lockout its teeth). **A) Last Stand HP:** `reactionSpeed(landMs, blockMs, windowMs = SPAWN_AHEAD_MS)` now divides by the passed window (default preserves old callers); `blockWindowForWave(wave, hpLevel = 0) = min(SPAWN_AHEAD_MS, base + hpLevel*20)`. LastStand.tsx `block()` accept filter, the `activeDirections` glow, and BOTH scoring calls (block + finish batch) now use `blockWindowForWave(a.wave, hpLevel)` — the narrowed accept window (750→500ms, not 1400) turns blind/early presses into whiffs that trip the lockout, and the matching scoring denominator drops masher mean reaction below the 3★ line. `hpLevel` prop from TrialModal (`statLevels.HP`; distinct from STARTING_HP=100 resource). **B) Rooftop AG:** new `ChaseState.dashCooldownBaseMs`, set in `initChase(rng, agLevel = 0) = max(1800, 2600 - 60*agLevel)`, read at the dash-reset site (was the bare `DASH_COOLDOWN_MS` constant) and used for the cooldown-fraction meter display; `agLevel` threaded `useChaseLoop(agLevel=0)` → RooftopChase → TrialModal. **C) Armory ST:** `armoryAccuracy(releasePos, zoneWidth = SWEET_ZONE_WIDTH)` scores against the passed width; ArmoryBreak.tsx computes per-lock `effectiveZoneWidth(lock, stLevel) = min(2·base, base + 0.006*ST)` (base [0.20,0.20,0.15]) used for BOTH the scoring call AND the `<MashMeter>` visual — **fixes a latent bug** where lock 3's narrower visual zone wasn't reflected in scoring (scoring always used the fixed 0.20). `stLevel` prop from TrialModal. **D) TrialModal:** added AG/ST/HP selectors (mirror the `?? 0` pattern), threaded through `GameComponent`, added three intro info boxes (cyan/rose/emerald) showing the live dash-cooldown / sweet-zone / block-window values. DEVIATIONS: ST widening capped at 2× base (the brief's "your call" option), constant 0.006/level; Armory MashMeter now draws every lock's effective widened zone (needed so shown width == scored width). Tests (trials.test.ts): blockWindowForWave HP-widening/cap/wave-clamp + a "accept window < SPAWN_AHEAD_MS" teeth assertion; reactionSpeed windowMs denominator (700ms out → >0.9 at a 750 window vs ~0.5 at the old 1400 default); initChase AG cooldown −60/lvl w/ 1800 floor + a dash resets to the run base (via stepChase); armoryAccuracy wider-zone scores a base-width miss while centre still peaks at 1.0. Each new assertion counterfactually fails against the old constant/default-width behaviour. 177 trials tests pass, tsc clean. No dedicated rooftopChase/armoryBreak test files exist (all trial tests live in trials.test.ts); spiritGrove.test.ts untouched. |
| 2026-07-07 | 6.4 | MINI-13 (P2) | src/engine/trials/longMarch.ts, src/components/trials/games/LongMarch.tsx, src/components/trials/TrialModal.tsx, src/engine/trials/__tests__/trials.test.ts | DELEGATE-opus (self-contained; engine + 2 components, no store/net/persist → tests+typecheck, no verifier). The EN trial had two bugs: (1) the component clamped every step to `MARCH_MAX_STA=12`, discarding the up-to-18 EN starting buffer on the first action (advertised +6 was effectively +1..+3); (2) `marchScore`'s 0.7 tile / 0.3 dist weighting made completion a 0.70 floor (2★ free) and a near-free 3★ (distance ≈5.4 of 32 sufficed), while the practical ceiling ≈0.855 meant the trial could never pay the full shared multiplier. **DESIGN LOCKED IN MAIN SESSION** (scout confirmed the march is tile-limited with NO turn budget → the finding's "Rest-doesn't-advance" alt would create an infinite-rest loop; rejected). **Fix:** new pure `marchStaminaCap(startStamina) = max(MARCH_MAX_STA, startStamina)` (engine) raises the stamina ceiling to the run's start value for the whole march (EN-18 → 18-tank all run → more Pushes → more distance = a real, distance-linked EN payoff); component clamp switched to `Math.min(marchStaminaCap(startStamina), max(0, …))`. New `MARCH_SCORE_DISTANCE = 20` (achievable strong-run distance); `marchScore = 0.5·min(1,tiles/16) + 0.5·min(1,dist/20)`. Star math (cutoffs 3★≥0.75, 2★≥0.40): rest-spam (16,0)=0.50 (2★, was 0.70 near-3★); distance 10 = 0.75 (3★ boundary — 3★ now requires genuine pace management); distance 5 = 0.625 (2★, closing the old ≈0.747 near-free 3★); max run (≥20) = 1.0 (fixes the 0.855 ceiling → full multiplier). Intro copy reworded to honest "max stamina (cap)" phrasing (the old "18 total" is now truthful since the buffer no longer vanishes). `MARCH_MAX_DISTANCE` (=MARCH_TILES*2=32) KEPT as a theoretical-max reference constant, no longer consumed by scoring. Tests: marchStaminaCap floors (12→12, 18→18, 10→12); marchScore rest-spam 0.50 / dist-10 0.75-boundary / dist-5 0.625 non-vacuous (comment notes old formula ≈0.747 there) / max-run 1.0 / cap-at-1. 181 trials tests pass, tsc clean. **Hard mode** (deferred, plan 2.2) reconsidered: this scoring fix UNBLOCKS it but building a new difficulty tier is a separate feature — left deferred, not required for Phase 6 acceptance. |
| 2026-07-07 | 6.5 | MINI-14 (P2), MINI-34 (P3) | src/content/trials.ts, src/components/trials/games/RoyalCourt.tsx, src/engine/__tests__/royalCourt.test.ts | DELEGATE-opus (content data + component; no store/net/persist → tests+typecheck, no verifier). **MINI-14 (gambit EV):** Royal Court gambits (d20 + raw CH ≥ DC, nat-20/nat-1 override) were −EV until CH 9–13 and two were strictly dominated (Voss/Physician gambit favorDelta == their safe max == 3). **EV DESIGN LOCKED IN MAIN SESSION** (break-even solved as `P·success + (1−P)·fail > safeMax`). Raised the 7 gambit success payoffs / softened fails (Herald 4→6/−3→−2, Aldric 4→6/−3→−2, Jester 4→6/−2→−1, Kessir 4→5/−2, Voss **3→6**/−2, Rhovas 4→6/−2, Physician **3→6**/−3→−2); DCs unchanged. Break-evens now CH ~1 (Kessir easy), ~4–5 (medium), ~8 (hard) — a live +EV bet at realistic CH. KEY MECHANICAL LEVER: score = `favor / maxFavor` where `maxFavor = Σ max(favorDelta) per drawn exchange`, so raising a gambit above its safe max also raises that exchange's denominator → safe-only play now caps ≈0.67 (was ~0.875, a near-guaranteed 3★) → 3★ now requires engaging the CH gambit mechanic (satisfies Phase 6 acceptance). All successes (5–6) > safe (2–3) = no dominated gambit; fails (−1/−2) < safe = both choices stay live. Intro copy already accurate → unchanged. Score/maxFavor code derives automatically → royalCourt.ts untouched. **MINI-34 (timer soft-lock):** RoyalCourt scheduled setInterval + nested setTimeout chains (ROLL_ANIM 650 → ADVANCE_DELAY 700 → TRANSITION 180) with NO unmount cleanup, so the deferred onFinish→completeTrial fired up to ~1350ms post-choice — Abandon→Yes in that window silently stamped the daily gate, spent energy, banked an unshown reward. Mirrored LastStand.tsx:67-76's proven idiom: `pendingTimers` ref + `scheduleTimeout` wrapper + unmount `useEffect` clearing all pending timeouts AND `rollIntervalRef`; routed all 5 setTimeouts through it; no timing/happy-path change. Tests: no-dominated-gambit invariant over the pool (non-vacuous — fails against old Voss/Physician 3==3); medium-DC EV sanity enumerating all 20 rolls (Herald EV 3.6 at CH6 > 3, 1.2 at CH0 < 3, no Math.random). 13 royalCourt tests pass, tsc clean. COVERAGE GAP (accepted): MINI-34 (P3) has no automated test — no RTL/component harness exists for any trial component in the repo; not built per scope; fix faithfully mirrors the LastStand idiom. |
| 2026-07-07 | 6.6 | MINI-15 (P2), MINI-16 (P2) | src/engine/trials/lockpicking.ts, src/engine/trials/spiritGrove.ts, src/content/trials.ts, src/store/slices/trialsSlice.ts, src/store/slices/coreSlice.ts, src/store/shared.ts, src/store/useGameStore.ts, src/components/trials/games/SpiritGrove.tsx, src/components/trials/TrialModal.tsx, src/engine/trials/__tests__/{trials,spiritGrove}.test.ts, src/store/__tests__/store.integration.test.ts | SPLIT: MINI-15 DELEGATE-sonnet (self-contained, no persist), MINI-16 DELEGATE-opus + **fix-verifier (persist-touching P2)**. **MINI-15 (lockpicking ceiling):** `lockTolerance` grew tolerance/openTolerance uncapped with level+DX (Adept lock → ~24° = 27% of the 180° arc by ~L15/DX15 → automatic daily 3★). Both return values now `Math.min(computed, 2 * BASE_*_DEG[lockIndex])` — capped at 2× per-lock base, so late locks stay bounded; low/mid unaffected (min only bites past the cap). Tests: cap exact at L30/DX30 (Adept tol=22, open=7) + non-vacuous mid-value uncapped (L3/DX2 = 12.8/4.1). **MINI-16 (Grove recall decay + mastery carrot), persist v31→v32:** (a) added a required stable `id` to `SpiritGroveRound` + unique difficulty-prefixed slugs on all **30** rounds (pool is 30/10-10-10 per the finding — the file's "Pool of 15" header comment was stale; scout misread it, implementer caught via tsc on the required field; corrected the comment). New persisted `spiritGroveSeen: string[]` (round ids shown to the player): added to TrialsSlice interface+init+`markSpiritGroveSeen` action, the hand-written GameState (field AND action — both required or tsc breaks), resetGame, and useGameStore (version 32, v32 comment, migrate `?? []`; NO merge line — an array is carried by `{...current,...p}` exactly like the trialAttemptNonce scalar, verifier-confirmed no data-loss path incl. cloudSave durable round-trip). `prepareRounds` gained `seen?: ReadonlySet<string>`; `byDiff` + extras padding order unseen-before-seen while preserving rng shuffle within each partition (deterministic; all-seen → full shuffle fallback). SpiritGrove.tsx passes `seen: new Set(spiritGroveSeen)` into the draft memo (NOT a dep — draft stable within an attempt; component unmounts to the result stage before seen updates) and marks drafted ids seen ONLY on completion (not per-round, not on abandon — keeps it separate from 6.7's free-abandonment reroll concern). (b) mastery gold ×1.15 in completeTrial gated on `spirit_grove && pre-update bestTrialScore>=1` (matches the component's `harder: best>=1` mastery-draft signal; perfect=5/5=exactly 1.0, no float edge). **Verifier SHOULD-FIX (fixed in-item):** TrialModal recomputed `trialReward` for the Reward panel and didn't apply the ×1.15 → mastery players were granted +15% but shown base gold (label under-promised the carrot). Fixed by snapshotting `wasMasteryRun` (pre-run best>=1, mirroring completeTrial's gate) in handleFinish like isNewBest, and multiplying the displayed gold to match the grant. Tests: unseen-bias non-vacuous (seed 2, seen={e0..e3} → unbiased picks a seen round, biased picks the lone unseen e4), determinism, all-seen fallback, dup-id assertion; store markSpiritGroveSeen union/dedup, mastery ×1.15 gold (base 14→16 at L1/0.6, Grove-only, non-Grove/non-mastery unaffected), version→32. 406 tests across the 3 touched suites pass, tsc clean. Verifier PASS-WITH-NITS (persist trace clean; the one nit fixed). |
| 2026-07-07 | 6.7 | BAL-20 (P3), MINI-40 (P3), + 6.2 free-abandonment carry-forward | src/engine/trials/trials.ts, src/engine/trials/armoryBreak.ts, src/store/slices/trialsSlice.ts, src/store/shared.ts, src/components/trials/TrialModal.tsx, src/components/trials/games/{LongMarch,ArmoryBreak}.tsx, src/store/__tests__/store.integration.test.ts, src/engine/trials/__tests__/trials.test.ts | SPLIT into two disjoint parallel DELEGATE-opus (A store/flow, B polish); P3 → tests+typecheck, no verifier (the store-energy relocation is covered by explicit no-double-charge + abandon-no-refund tests). **DESIGN LOCKED IN MAIN SESSION:** charge trial energy on **Begin, not completion** — matching the house idiom (`beginMining`/arena/dungeon/forest all debit at entry). One change closes three things at once. **A) beginTrial (charge-on-start):** trialsSlice now `(set, get)`; new `beginTrial(trialId)` evaluates the 3 gates (daily-clear / energy / stat) and on success debits 1 energy (+per-day `energySpentPatch` ledger) and bumps `trialAttemptNonce`, returning a typed `TrialBeginResult = {ok:true} | {ok:false, reason:'cleared'|'energy'|'stat'}` (new type in trials.ts). **Removes** `startTrialAttempt` (fully subsumed). `completeTrial` **drops** the energy + stat gates and the energy spend (all now at Begin), **keeps** only the daily-clear check as an idempotency guard, and **returns boolean `banked`**. GameState (hand-written) updated: `completeTrial → boolean`, removed startTrialAttempt, added beginTrial + TrialBeginResult import. **BAL-20 fix:** the gate is now the visible Begin button (TrialModal calls beginTrial, shows an inline refusal message per reason instead of silently entering play), and completeTrial's boolean drives an honest "not banked" result-stage note (was: silent post-play no-op that still showed a reward). **6.2 free-abandonment closure:** because every Begin costs 1 energy, reopening a deterministic trial (Grove/Library) to fish a familiar draft is no longer free. **MINI-40(a) copy:** TrialModal "Daily free attempt" → "Costs 1 ⚡ energy"; trials.ts docstring "for free" → "(costs 1 energy)". NO persist bump (reuses character.energy + trialAttemptNonce). **B) MINI-40(b) Long March:** deleted the unreachable `done ? … : deadScreen` else-branch (onFinish unmounts synchronously → the March-Again screen never painted) + now-orphaned `reset`/`collapsed`/`newRun`/`setTerrain`; KEPT the `{!done && …}` pace-button guard (load-bearing — the map reads `tile.kind` unguarded and `done` flips true when tileIndex hits MARCH_TILES). **MINI-40(c) Armory:** new pure `projectReleasePower(power, riseSpeed, dtSeconds) = clamp01(power + riseSpeed·max(0,dt))`; handleRelease captures `performance.now()`, computes `dtSeconds = max(0,(releaseTs − lastTs)/1000)`, and scores off the projected power so a well-timed release isn't quantized to the previous frame (~0.84 → ~1.0). `effectiveZoneWidth` inputs to armoryAccuracy/MashMeter unchanged — only the power arg moved raw→projected. Tests: beginTrial deducts-1 / no-op-at-0 / unlimitedEnergy-bypass / advances-nonce, completeTrial no-double-charge + no-longer-stat-gates + returns banked, new "charge on start / gate honesty (6.7)" describe (begin→abandon-no-refund, banked true/false, reason:'cleared'); projectReleasePower dt=0/raise-by-exact/clamp/neg-dt. 376 tests across both suites pass, tsc clean. |
| 2026-07-07 | 6.8 | MINI-35 (P3) | src/engine/trials/rooftopChase.ts, src/components/trials/games/RooftopChase.tsx, src/components/trials/TrialModal.tsx, src/engine/trials/__tests__/trials.test.ts, src/engine/__tests__/rooftopChase.test.ts | DELEGATE-opus (self-contained; engine + component + copy, no store/net/persist — ChaseState is a useRef, never persisted → tests+typecheck, no verifier). Scout confirmed both premises hold on the CURRENT (6.3-modified, AG-aware) file. **DESIGN LOCKED IN MAIN SESSION.** Two problems: the crossbowman was a reskinned lowbar (`resolveContact` treated it identically — slide-only, jump never cleared → no input variety), and dash-spamming netted +0.9..+4.4 wu/s of lead so a disciplined dasher was never "caught" (falls were the only fail state). **Part 1 (marginal dash cost):** `DASH_LEAD_GAIN` 16→**12** — one dash per base 2600ms cooldown now nets ≈ 12 − CHASER_GAIN_PER_SEC(4.5)×2.6 = +0.3 wu/cooldown (≈+0.1/s), which turns net-NEGATIVE once SURGE_REAL_DRAIN(3) fires past 400wu → a pure dash-spammer is now caught unless they also stomp mooks/slide (engaging mechanics is rewarded). Deliberately did NOT touch `DASH_COOLDOWN_MS`/`dashCooldownBaseMs` (6.3's AG tuning) — the AG-floored 1800ms case stays net-positive (12−4.5×1.8 ≈ +2.2/s = the intended high-AG reward). Preserved the `DASH_LEAD_GAIN > STOMP_LEAD_GAIN(9)` invariant (12>9). **Part 2 (jumpable bolt = real input variety):** new `PropKind 'bolt'`; `resolveContact('bolt') = grounded ? 'stumble' : 'clear'` (jump to clear — the mechanical INVERSE of the slide-only crossbowman body). `generateCourse` pairs a bolt (width 2wu) 5wu BEFORE every crossbowman body (3wu clearance), so the crossbowman now demands a **jump (bolt) then slide (body)** combo; only spawns the pair when the building's `[+3,−3]` safe zone spans ≥7.5wu (buildingWidth ≳13.5), else falls back to a lone crossbowman — generated from the same seeded rng so the course stays deterministic. Crossbowman body case UNCHANGED (slide-only); exhaustive `never` default kept (new case required or tsc breaks). Renderer: new horizontal `BoltSprite` (low-flying projectile) + a "jump here" telegraph nub mirroring the elevation-telegraph nub pattern (render-only, no new engine timing). **Part 3 (copy honesty):** updated all 3 stale obstacle-list copy sites (TrialModal rooftop_chase description + both RooftopChase in-run tip strings) to name the crossbowman + its jumpable bolt. Score (`chaseScore=distance/600`) untouched. Tests: `resolveContact — bolt` (airborne→clear, grounded→stumble, grounded+sliding→stumble); dash-economy regression (net dash-only lead over one base cooldown ≤~0 — counterfactually fails at 16, passes at 12; AG-floored 1800ms stays positive); crossbowman↔bolt pairing + determinism; `'bolt'` added to the valid-kind Set. Existing crossbowman-body block + 6.3 AG-cooldown tests untouched and green. 283 tests across the 2 suites pass, tsc clean. **PLAYTEST CAVEAT (deferred, non-blocking):** the fixed jump arc (~1.375s) makes the land-then-slide window on a bolt+body pair tight in live play — a tuning concern outside this test-gated engine item; flag for the check-6 manual playtest. |
| 2026-07-07 | 7.1 | ARCH-06, ARCH-02, ARCH-03, ARCH-04, ARCH-05 (all P2) | src/engine/crawl.ts, src/engine/mining.ts, src/engine/forest.ts, src/engine/__tests__/{mining,forest}.test.ts | DIRECT-design (main session locked the interface + all reconciliation decisions from a full finding-scout brief) → DELEGATE-opus mechanical hoist → **fix-verifier (opus, correctness-critical despite engine-only diff — a ~140-line castSpell merge is where silent behavior loss hides)**. Highest-blast-radius engine refactor: mining.ts/forest.ts were ~90%-identical hand-maintained twins. Hoisted 5 functions into crawl.ts as generics over `<TState extends CrawlRunState, TUnit extends CrawlUnit>` behind minimal per-function **callback bags** (`CrawlUnitCaps`/`CrawlSpellCaps`/`CrawlRuneCaps`/`CrawlContactCaps`/`CrawlBoonCaps`) — `crawlApplyBoonChoice`, `crawlDamageUnitById`, `crawlCoopClientStep`, `crawlCastSpell`, `crawlTriggerRunes`; each engine keeps its EXACT exported wrapper (store slices + net/coop/reduce.ts call sites unchanged; `damageMonsterById`/`damageBeastById` names preserved). crawl.ts stays pure — imports only `./stats`+`./spells`+`./combat` (engine siblings, no cycle), zero `@/content`; all monster/beast content flows through caps (layering guard green). Shared `CrawlUnit` carries only common fields; `asleep`/`windupUntilMs`/`flees` never enter it (live inside `canStrike`/`unitDef`). **ARCH-06 structural payoff:** single shared body ⇒ **ARCH-02** (forest runes now pruned on `expiresAtMs>nowMs` UNCONDITIONALLY — dropped forest's `if(triggered.size>0)` gate; rune-to-player ward mitigation preserved) and **ARCH-03** (forest `castSpell` now has mine's `knownSpells` guard, positioned before any mp/cooldown mutation) are fixed for BOTH crawlers at once. **ARCH-04 (design decision, locked to mine's formula):** all three contact sites (mine stepMonsters = reference/unchanged; forest stepBeasts; forest coopClientStep) now use `max(1, raw - defense - (bless?bless.magnitude:0) - boonDefenseBonus)`. **Deliberate balance change — forest `ward` no longer mitigates CONTACT damage** (still mitigates rune/spell hits); baseline defense now always applies (was bless-gated in forest) and bless stacks on top. Comment documents it at the forest site. **ARCH-05:** `newOccupied` folded into each unit's `blocked` set (`for (const k of newOccupied) blocked.add(k)`) before flowStep/fleeStep in BOTH stepMonsters + stepBeasts, so two units can't path onto one cell per tick; delete/add bookkeeping + forest fleeStep/playerCells guard intact. **Forks reconciled (beyond the named findings):** (a) castSpell targeting kept per-engine via `preferFaced` cap (mine faced-then-nearest; forest nearest-only — NOT changed); (b) forest teleport fog-reveal preserved via optional `afterTeleport` cap (forest passes `reveal`, mine omits); (c) coopClientStep toucher predicate via `canStrike(unit,nowMs)` cap (mine frozen-only; forest asleep/flees/touchDamage/frozen). **VERIFIER PASS-WITH-NITS → both nits dispositioned as FALSE POSITIVES:** verifier diffed against `HEAD` (last commit) not the pre-hoist working tree; the "silently added" `lateDepthDamageScale` on forest+mine contact it flagged is item **5.4's** logged, intended balance change (added to both crawlers' contact reads "in lockstep" 2026-07-07, pre-hoist & uncommitted) — 7.1 faithfully preserved it, confirmed via the scout's verbatim pre-hoist excerpts + `git show HEAD` (which lacks it) + the 5.4 log row. Merge fidelity itself: verifier traced every castSpell branch (rune fire/ice/poison, ring-of-fire, teleport, damage+burn/poison/freeze, heal+bless, illusion) reproduced identically both engines with identical mpCost/CD/magnitudes/rng-order — no effect dropped, no rng draw reordered (the one illusion `/8→/4` delta is 5.7/BAL-07's intended change). New tests (non-vacuous, concrete pre/post: ARCH-04 defense 48 vs pre-fix 46, ward-removed 46 vs pre-fix 49; ARCH-02 expired-rune len 0 vs kept; ARCH-05 no-stack). Full suite 60 files **1590 passed** (+13 vs pre-item), tsc clean, no persist/PROTOCOL bump (state shapes unchanged). |
| 2026-07-06 | migrations (ad-hoc) | 0007, 0008 | supabase/migrations/0008_consistency_leaderboard.sql | Connected Supabase MCP (project rclrnxeazvlqenskaqzv) and reconciled applied-vs-repo migration state. Introspection found 0007 (member_habits table) and 0008 (profiles.habit_score + leaderboard view) NOT live despite files existing — both are read by the client (party.ts getMembers/getLeaderboard, cloudSave.ts member_habits upsert), so party habit-sharing + the Consistency leaderboard track were runtime-broken. 0010/0011/0009 confirmed already live. Root cause of 0008 never applying: its `create or replace view` RENAMED columns (mine_floor/tactics_tier) and RETYPED total_xp (numeric→bigint) — both forbidden (ERROR 42P16), and the renamed names never matched the client's LeaderboardRow (deepest_mine/deepest_forest/deepest_arena) anyway; the live view predates 0005's rename too. Applied via MCP apply_migration: 0007 (table+RLS+2 policies), corrected 0008 (habit_score generated col + index + view APPEND of habit_score onto the live shape). Verified all 6 objects present + view columns 1-9 match LeaderboardRow. Patched repo 0008 file → drop+create with client-correct column names (fresh-deploy-safe, documented the 42P16 correction). Security advisor after DDL: member_habits clean (RLS enforced); pre-existing security_definer_view ERROR on leaderboard is intentional (cross-user board needs it) and was not introduced by this change. No code/test changes to app source; not a plan3 item (no marker flip). |
